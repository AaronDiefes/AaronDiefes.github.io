version: '3.8'

services:
  portfolio:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: aarondiefes-portfolio
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge
