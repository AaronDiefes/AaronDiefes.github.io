---
phase: 08-visual-polish
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - index.html
  - wasm-graphics-demo.html
  - graphics-demo.html
  - admin.html
  - docs/index.html
  - docs/core-rendering.html
  - docs/transforms-textures.html
  - docs/paths-gradients.html
  - docs/advanced-geometry.html
  - docs/final-features.html
  - docs/optimization-performance.html
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Users can navigate between portfolio, demo, and documentation from every page"
    - "Mobile users see a hamburger menu that opens/closes with proper ARIA attributes"
    - "Graphics engine pages link to the graphics engine repo, not the portfolio repo"
    - "No redundant duplicate links to the same page appear in navigation"
    - "Old per-page navigation elements are removed"
  artifacts:
    - path: "index.html"
      provides: "Homepage with site-nav linking to portfolio repo"
      contains: "site-nav"
    - path: "wasm-graphics-demo.html"
      provides: "Demo page with site-nav linking to graphics-engine repo"
      contains: "site-nav"
    - path: "docs/index.html"
      provides: "Docs landing with site-nav"
      contains: "site-nav"
  key_links:
    - from: "docs/assets/js/nav.js"
      to: "all HTML pages"
      via: "script tag"
      pattern: "nav\\.js"
    - from: "docs/assets/css/nav.css"
      to: "all HTML pages"
      via: "link rel=stylesheet"
      pattern: "nav\\.css"
    - from: "navigation links"
      to: "correct repositories"
      via: "href attributes"
      pattern: "github\\.com/AaronDiefes"
---

<objective>
Add consistent site-wide navigation to all pages and clean up old navigation elements.

Purpose: Users need a clear, consistent way to navigate between the portfolio homepage, demo pages, and documentation from any page. Mobile users need the hamburger menu from 08-03. Links to source code must point to the correct GitHub repository. Old per-page navigation must be removed.

Output: All 11 pages updated with consistent nav bar, correct repo links, and old navigation removed.
</objective>

<execution_context>
@/Users/orases/.claude/get-shit-done/workflows/execute-plan.md
@/Users/orases/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-visual-polish/08-CONTEXT.md
@.planning/phases/08-visual-polish/08-02-SUMMARY.md
@.planning/phases/08-visual-polish/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add navigation HTML, CSS link, and JS script to all pages</name>
  <files>
    index.html
    wasm-graphics-demo.html
    graphics-demo.html
    admin.html
    docs/index.html
    docs/core-rendering.html
    docs/transforms-textures.html
    docs/paths-gradients.html
    docs/advanced-geometry.html
    docs/final-features.html
    docs/optimization-performance.html
  </files>
  <action>
Add navigation assets and HTML to ALL 11 user-facing pages.

**For root-level pages, add to `<head>`:**
```html
<link rel="stylesheet" href="docs/assets/css/nav.css">
```
And before `</body>`:
```html
<script src="docs/assets/js/nav.js"></script>
```

**For docs/ pages, add to `<head>`:**
```html
<link rel="stylesheet" href="assets/css/nav.css">
```
And before `</body>`:
```html
<script src="assets/js/nav.js"></script>
```

**Navigation HTML structure** (insert at the very beginning of `<body>`, BEFORE any existing header):

```html
<nav class="site-nav" aria-label="Main navigation">
  <a href="{path-to-index}" class="nav-brand">{brand-text}</a>
  <button class="menu-toggle" aria-expanded="false" aria-controls="nav-menu">
    <span class="sr-only">Toggle menu</span>
    <span class="hamburger-icon"></span>
  </button>
  <ul id="nav-menu" class="nav-links" hidden>
    <li><a href="{path-to-index}">Portfolio</a></li>
    <li><a href="{path-to-demo}">Demo</a></li>
    <li><a href="{path-to-docs}">Documentation</a></li>
    <li><a href="https://github.com/AaronDiefes/{correct-repo}" target="_blank">Source Code</a></li>
  </ul>
</nav>
```

**Page-specific link values:**

| Page | Brand | Portfolio href | Demo href | Docs href | Source Code repo |
|------|-------|---------------|-----------|-----------|-----------------|
| index.html | Aaron Diefes | # (active) | wasm-graphics-demo.html | docs/index.html | AaronDiefes.github.io |
| wasm-graphics-demo.html | Graphics Engine | index.html | # (active) | docs/index.html | graphics-engine |
| graphics-demo.html | Graphics Engine | index.html | wasm-graphics-demo.html | docs/index.html | graphics-engine |
| admin.html | Graphics Engine | index.html | wasm-graphics-demo.html | docs/index.html | graphics-engine |
| docs/index.html | Graphics Engine | ../index.html | ../wasm-graphics-demo.html | # (active) | graphics-engine |
| docs/*.html (6 pages) | Graphics Engine | ../index.html | ../wasm-graphics-demo.html | index.html | graphics-engine |

Mark the current page's nav link with `class="active"` and `aria-current="page"`. Remove the `hidden` attribute from `<ul>` on desktop via CSS (the `hidden` attribute is only for the mobile closed state and is managed by JS).
  </action>
  <verify>
Every page has `<nav class="site-nav">` as the first element in `<body>`. Every page links to nav.css and nav.js. The current page's nav link has `class="active"` and `aria-current="page"`.
  </verify>
  <done>
All 11 pages have consistent top navigation with nav.css/nav.js linked and correct page-specific hrefs and active states.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove old navigation and optimize links</name>
  <files>
    index.html
    docs/index.html
    docs/core-rendering.html
    docs/transforms-textures.html
    docs/paths-gradients.html
    docs/advanced-geometry.html
    docs/final-features.html
    docs/optimization-performance.html
  </files>
  <action>
**Remove existing navigation elements that the new site-nav replaces:**

- `docs/core-rendering.html`: Remove the `<nav class="docs-nav">` inside `<header class="docs-header">` (the header itself with h1/subtitle stays)
- `docs/transforms-textures.html`, `docs/paths-gradients.html`, `docs/advanced-geometry.html`, `docs/final-features.html`: Remove the `<nav class="docs-nav" style="...">` block with its inline-styled links inside the header
- `docs/optimization-performance.html`: Remove the `<nav class="breadcrumb">` if it duplicates the navigation links (keep breadcrumb if it shows page hierarchy like "Docs > Optimization")
- `docs/index.html`: Remove the `<nav class="landing-nav">` inside the landing-header (the header h1/subtitle stays)

**Link optimization (per user decision):**
- On `index.html`: Remove the "JS Simulation" and "View Source Code" secondary buttons from the featured project card (the top nav now provides access to source code; the JS simulation is secondary to the WASM demo). Keep the "Try Real C++ Engine" and "View Documentation" buttons as primary CTAs.
- On demo pages, do NOT link back to "Demo" in the nav -- it is marked as active instead.
- On documentation pages, keep breadcrumbs only if they show page hierarchy, not if they duplicate the top nav.

**Fix repository link accuracy (per user decision):**
- Verify all engine-related pages (demo, admin, docs) use `https://github.com/AaronDiefes/graphics-engine` for "Source Code" in nav
- Verify portfolio page uses `https://github.com/AaronDiefes/AaronDiefes.github.io` for "Source Code" in nav
- Check for any OTHER GitHub links in page content that point to wrong repo and fix them
  </action>
  <verify>
1. No duplicate navigation elements remain (no `docs-nav`, `landing-nav`, or `breadcrumb-as-nav`)
2. Engine-related pages' "Source Code" nav link points to `github.com/AaronDiefes/graphics-engine`
3. Portfolio "Source Code" nav link points to `github.com/AaronDiefes/AaronDiefes.github.io`
4. No redundant secondary links on index.html project card
  </verify>
  <done>
Old navigation elements removed. Redundant links eliminated from project card. All "Source Code" links point to correct repositories.
  </done>
</task>

</tasks>

<verification>
1. Open each page in browser and verify site-nav bar appears at top
2. Resize to 768px width - hamburger button appears, clicking it opens menu
3. Press ESC with menu open - menu closes and focus returns to toggle button
4. Tab through nav links - focus rings visible, Enter activates link
5. Click outside open menu - menu closes
6. Check "Source Code" link on wasm-graphics-demo.html -> points to graphics-engine repo
7. Check "Source Code" link on index.html -> points to portfolio repo
8. No old navigation elements (docs-nav, landing-nav, breadcrumb-as-nav) remain
</verification>

<success_criteria>
- Consistent navigation bar on all 11 user-facing pages
- Hamburger menu appears at 768px breakpoint with proper ARIA
- Engine pages link to correct graphics-engine GitHub repo
- Portfolio page links to correct portfolio GitHub repo
- No redundant duplicate links within a single page
- Old per-page navigation markup removed
</success_criteria>

<output>
After completion, create `.planning/phases/08-visual-polish/08-04-SUMMARY.md`
</output>
