---
phase: 08-visual-polish
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - index.html
  - wasm-graphics-demo.html
  - graphics-demo.html
  - admin.html
  - docs/index.html
  - docs/core-rendering.html
  - docs/transforms-textures.html
  - docs/paths-gradients.html
  - docs/advanced-geometry.html
  - docs/final-features.html
  - docs/optimization-performance.html
  - docs/assets/css/docs.css
  - docs/assets/css/code.css
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Every page uses the unified green color palette with no purple/blue (#667eea, #764ba2) remaining"
    - "CSS files (docs.css, code.css) use var(--color-*) references instead of hardcoded hex where possible"
  artifacts:
    - path: "index.html"
      provides: "Portfolio homepage with green palette"
      contains: "#2E7D32"
    - path: "wasm-graphics-demo.html"
      provides: "WASM demo page with green palette"
      contains: "#2E7D32"
    - path: "docs/assets/css/docs.css"
      provides: "Doc styles using CSS variables"
      contains: "var(--color-primary)"
    - path: "docs/assets/css/code.css"
      provides: "Code styles using CSS variables"
      contains: "var(--color-primary)"
  key_links:
    - from: "docs/assets/css/docs.css"
      to: "design-system.css"
      via: "var(--color-*) references"
      pattern: "var\\(--color"
    - from: "docs/assets/css/code.css"
      to: "design-system.css"
      via: "var(--color-*) references"
      pattern: "var\\(--color"
---

<objective>
Migrate all pages from purple/blue to the unified green color palette.

Purpose: Replace every instance of the old purple/blue gradient colors (#667eea, #764ba2) with the new green palette (#2E7D32, #1B5E20) defined in design-system.css. Update CSS files to use CSS custom property references where possible.

Output: Zero purple/blue colors remaining across all user-facing pages and CSS files.
</objective>

<execution_context>
@/Users/orases/.claude/get-shit-done/workflows/execute-plan.md
@/Users/orases/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-visual-polish/08-CONTEXT.md
@.planning/phases/08-visual-polish/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate purple/blue to green across all HTML pages</name>
  <files>
    index.html
    wasm-graphics-demo.html
    graphics-demo.html
    admin.html
    docs/index.html
    docs/core-rendering.html
    docs/transforms-textures.html
    docs/paths-gradients.html
    docs/advanced-geometry.html
    docs/final-features.html
    docs/optimization-performance.html
  </files>
  <action>
Replace ALL instances of purple/blue with green equivalents across every user-facing HTML page:

| Find | Replace With |
|------|-------------|
| `#667eea` (all occurrences, case-insensitive) | `#2E7D32` |
| `#764ba2` (all occurrences, case-insensitive) | `#1B5E20` |
| `linear-gradient(135deg, #667eea 0%, #764ba2 100%)` | `linear-gradient(135deg, #2E7D32 0%, #1B5E20 100%)` |
| `rgba(102, 126, 234, 0.3)` (purple shadow on project-card hover) | `rgba(46, 125, 50, 0.3)` |

**Specific files and their changes:**

**index.html** (~4 replacements):
- Header gradient -> green gradient
- `.project-card:hover` box-shadow rgba -> green rgba
- Any other purple/blue hex occurrences

**wasm-graphics-demo.html** (~12 replacements):
- body background gradient -> green gradient
- `.badge` background -> `#2E7D32`
- `button` background -> `#2E7D32`
- `.status` border-left -> `#2E7D32`
- `.status h3` color -> `#2E7D32`
- `#statusDetails a` color -> `#2E7D32`
- Loading bar border-top -> `#2E7D32`
- `.info-box` border-left -> `#2E7D32`
- `.info-box h3` color -> `#2E7D32`

**graphics-demo.html** (~4 replacements):
- body background gradient -> green gradient
- `button` background -> `#2E7D32`
- `.shape-label` color -> `#2E7D32`
- `.range-value` color -> `#2E7D32`
- Default color picker `value="#667eea"` -> leave as is (user-selectable drawing color, not theming)

**admin.html** (~5 replacements):
- body background gradient -> green gradient
- input focus border-color -> `#2E7D32`
- button background -> `#2E7D32`
- `.file-tree a` color -> `#2E7D32`
- `.viewer-btn` background -> `#2E7D32`

**docs/*.html pages** (7 pages):
- Search each for any remaining `#667eea` or `#764ba2` in inline styles and replace with green equivalents

**Do NOT modify:**
- `graphics-demo.html` color picker input `value="#667eea"` (that is a default drawing color, not theming)
- Test pages (`wasm-test.html`, `wasm-test-phase2.html`, `wasm-test-images.html`)
  </action>
  <verify>
`grep -ri "#667eea" --include="*.html" . | grep -v wasm-test | grep -v 'value="#667eea"'` returns zero results.
`grep -ri "#764ba2" --include="*.html" . | grep -v wasm-test` returns zero results.
  </verify>
  <done>
Zero instances of #667eea or #764ba2 remain in any user-facing HTML file (except the graphics-demo.html color picker default value). All pages use unified green palette.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CSS files to use design system variables</name>
  <files>
    docs/assets/css/docs.css
    docs/assets/css/code.css
  </files>
  <action>
Update the two shared CSS files to replace hardcoded purple/blue colors with green, using CSS custom property references where possible:

**docs/assets/css/docs.css:**
- Replace `#667eea` with `var(--color-primary)` (or `#2E7D32` if inside a gradient that needs the hex)
- Replace `#764ba2` with `var(--color-primary-dark)` (or `#1B5E20`)
- `.demo-link` background and border -> use `var(--color-primary)` and hover state `var(--color-primary-dark)`
- Any other purple/blue hex values -> green equivalents using var() references

**docs/assets/css/code.css:**
- Copy button background -> `var(--color-primary)`
- Copy button hover -> `var(--color-primary-dark)`
- Any other purple/blue hex values -> green equivalents using var() references

Since these CSS files load after design-system.css (linked in Plan 08-01), they can safely reference `var(--color-*)` custom properties.
  </action>
  <verify>
`grep -ri "#667eea\|#764ba2" docs/assets/css/docs.css docs/assets/css/code.css` returns zero results.
`grep -c "var(--color" docs/assets/css/docs.css` returns at least 1.
`grep -c "var(--color" docs/assets/css/code.css` returns at least 1.
  </verify>
  <done>
docs.css and code.css use var(--color-*) references instead of hardcoded hex. Zero purple/blue colors remain in any CSS file.
  </done>
</task>

</tasks>

<verification>
1. `grep -ri "#667eea" --include="*.html" --include="*.css" . | grep -v wasm-test | grep -v 'value="#667eea"'` returns zero results
2. `grep -ri "#764ba2" --include="*.html" --include="*.css" . | grep -v wasm-test` returns zero results
3. docs.css and code.css contain `var(--color-primary)` or `var(--color-primary-dark)` references
4. Open index.html in browser to verify green palette renders correctly
</verification>

<success_criteria>
- Zero purple/blue (#667eea, #764ba2) colors remaining in user-facing pages (except color picker default)
- Unified green palette (#2E7D32, #1B5E20) used consistently site-wide
- docs.css and code.css updated to use CSS variable references where possible
</success_criteria>

<output>
After completion, create `.planning/phases/08-visual-polish/08-02-SUMMARY.md`
</output>
