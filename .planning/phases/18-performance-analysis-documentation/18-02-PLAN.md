---
phase: 18-performance-analysis-documentation
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/pages/cs330/Cs330PerformancePage.jsx
  - src/App.jsx
autonomous: true

must_haves:
  truths:
    - "Visitor can navigate to /projects/cs330/docs/performance and see Performance Analysis page"
    - "D1 comparison graph displays with real metrics from algorithm runs"
    - "D2 comparison graph displays with real metrics from algorithm runs"
    - "Combined D1/D2 bar chart displays with real performance data"
    - "Supply-demand scatter plot displays with real simulation data"
    - "Runtime comparison bar chart shows actual algorithm execution times"
    - "Algorithm comparison table shows real metrics from cs330_metrics.json"
    - "Page explains D1 and D2 metrics before showing graphs"
    - "Page uses forest green design system matching other CS330 pages"
    - "Page has breadcrumbs: Home > CS330 Case Study > Documentation > Performance Analysis"
  artifacts:
    - path: "src/pages/cs330/Cs330PerformancePage.jsx"
      provides: "Performance Analysis documentation page with real CS330 data"
      min_lines: 300
    - path: "src/App.jsx"
      provides: "Route registration for /projects/cs330/docs/performance"
      contains: "Cs330PerformancePage"
  key_links:
    - from: "src/pages/cs330/Cs330PerformancePage.jsx"
      to: "public/projects/cs330/images/*.png"
      via: "img src attributes"
      pattern: "src=\"/projects/cs330/images/"
    - from: "src/App.jsx"
      to: "src/pages/cs330/Cs330PerformancePage.jsx"
      via: "import and Route element"
      pattern: "import Cs330PerformancePage"
    - from: "src/pages/cs330/Cs330PerformancePage.jsx"
      to: "src/components/shared/Breadcrumbs.jsx"
      via: "import Breadcrumbs"
      pattern: "import Breadcrumbs"
---

<objective>
Create Performance Analysis React page documenting real CS330 algorithm performance with embedded graphs and metrics from actual simulation runs.

Purpose: Build Cs330PerformancePage.jsx that presents actual D1/D2 performance metrics from the CS330 repository algorithm simulations, embeds the 5 matplotlib-generated graphs showing real data, displays algorithm comparison table with actual measurements, and explains the performance evolution from T1 to T5 using authentic results.

Output: Performance Analysis page at `/projects/cs330/docs/performance` with real CS330 performance data, graphs, and metrics.
</objective>

<execution_context>
@/Users/orases/.claude/get-shit-done/workflows/execute-plan.md
@/Users/orases/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-performance-analysis-documentation/18-RESEARCH.md
@.planning/phases/18-performance-analysis-documentation/18-01-SUMMARY.md
@src/pages/cs330/Cs330AlgorithmPage.jsx
@src/App.jsx
@scripts/cs330_metrics.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cs330PerformancePage.jsx with real metrics, graphs, and tables</name>
  <files>src/pages/cs330/Cs330PerformancePage.jsx</files>
  <action>
    **First, load the real metrics:**
    Read `scripts/cs330_metrics.json` to get the actual D1, D2, and runtime values from the CS330 algorithm runs. The JSON structure should contain entries for each algorithm (T1-T5) with their measured performance data.

    **Then create** `src/pages/cs330/Cs330PerformancePage.jsx` following the exact same pattern as Cs330AlgorithmPage.jsx (inline CSS in style tag, Breadcrumbs import, forest green header, container/section structure, quick-links, 4-column footer).

    The page must include these sections in order:

    1. **Breadcrumbs**: `Home > CS330 Case Study > Documentation > Performance Analysis`

    2. **Header**: Forest green gradient header with title "Performance Analysis" and subtitle "Real Algorithm Performance: D1/D2 Metrics and Benchmarks"

    3. **Metrics Overview section**:
       - Define D1: Average match distance (cumulative passenger-driver distance / total rides)
       - Define D2: Maximum match distance (worst single match distance)
       - Explain D1 measures overall system efficiency
       - Explain D2 measures fairness (no rider gets stuck with extremely far driver)
       - Mention runtime (D3) as computational efficiency metric
       - Use `<span className="metric-badge">D1</span>` and `<span className="metric-badge">D2</span>` badges

    4. **D1 Analysis section**:
       - Embed `d1_comparison.png` graph with className="performance-graph"
       - Alt text: "Line graph showing D1 average match distance from real CS330 algorithm simulations, T1 through T5"
       - Explain improvements visible in the graph:
         - T1→T2: Minor improvement from early termination
         - T2→T3: Major jump from spatial partitioning (grid cells)
         - T3→T4: Significant improvement from KD-tree logarithmic search
         - T4→T5: Final refinement from road network distance vs Euclidean
       - Include actual D1 values from cs330_metrics.json in the text

    5. **D2 Analysis section**:
       - Embed `d2_comparison.png` graph
       - Alt text: "Line graph showing D2 maximum match distance from real CS330 algorithm simulations, T1 through T5"
       - Explain D2 fairness improvements:
         - Why D2 matters: prevents worst-case scenarios where one rider waits much longer
         - How spatial algorithms reduce D2 by finding closer matches
         - T5 advantage: road distance eliminates geographical proximity that's road-inaccessible
       - Include actual D2 values from cs330_metrics.json

    6. **D1 vs D2 Combined section**:
       - Embed `d1_d2_combined.png` graph
       - Alt text: "Grouped bar chart comparing D1 and D2 metrics across T1-T5 algorithms from real simulations"
       - Explain the D1-D2 gap:
         - Gap shows variance in match quality (wide gap = some matches much worse than average)
         - Gap narrows as algorithms improve (T1→T5)
         - T5 has smallest gap: road distance provides consistent accuracy for all matches

    7. **Runtime Comparison section**:
       - Embed `runtime_comparison.png` graph
       - Alt text: "Bar chart showing actual algorithm runtimes from CS330 simulations"
       - Explain the speed-accuracy tradeoff using real measured times:
         - T1-T3: Runtime decreases with better spatial pruning
         - T4: Fastest (KD-tree queries are very efficient)
         - T5: Slightly slower (adds Dijkstra pathfinding overhead)
         - Tradeoff discussion: T4 fast but uses Euclidean; T5 accurate but adds graph search cost
       - Include actual runtime values from cs330_metrics.json

    8. **Supply-Demand Distribution section**:
       - Embed `supply_demand_distribution.png` graph
       - Alt text: "Scatter plot showing spatial distribution of riders and drivers from real CS330 simulation data"
       - Explain why spatial distribution matters:
         - Real-world rider/driver locations are clustered (not uniform)
         - Spatial algorithms exploit this clustering
         - Visual shows why brute force is wasteful (many far-away drivers searched unnecessarily)

    9. **Algorithm Performance Summary section**:
       - HTML table with className="comparison-table"
       - Columns: Algorithm, Complexity, D1 (km), D2 (km), Runtime (ms), Key Technique
       - Green gradient header row
       - **CRITICAL:** Use the REAL values from `scripts/cs330_metrics.json` to populate the D1, D2, and Runtime columns
       - If the executor cannot read the JSON at component creation time (static site), include a note in the task action to manually inspect cs330_metrics.json and hardcode the actual values into the table
       - Complexity column (theoretical): T1: O(n×m), T2: O(n×m/k), T3: O(n×k), T4: O(n×log m), T5: O(n×(log m + E log V))
       - Key Technique column: T1: Exhaustive search, T2: Early termination, T3: Spatial partitioning, T4: Balanced tree search, T5: Road network distance

    10. **Key Takeaways section**:
        - Summarize insights from the REAL data (bullet list):
          - What was the biggest performance jump? (quantify using actual metrics)
          - Speed vs accuracy tradeoff (T4 vs T5 comparison)
          - When does spatial indexing provide value? (based on observed D1/D2 improvements)
          - Fairness vs efficiency (D1 vs D2 improvements)

    11. **Explore Further section**:
        - Link to Algorithm Evolution: `/projects/cs330/docs/algorithm`
        - Link to KD-Tree docs: `/projects/cs330/docs/kdtree`
        - Link to Pathfinding docs: `/projects/cs330/docs/pathfinding`
        - Link to CS330 GitHub repo: `https://github.com/AaronDiefes/cs330-case-study` (target="_blank" rel="noopener noreferrer")
        - Link back to CS330 docs landing: `/projects/cs330/docs`

    12. **Footer**: Exact same 4-column footer pattern as Cs330AlgorithmPage.jsx (Aaron Diefes bio, Navigation links including Performance Analysis as current page, Resources with GitHub links, Built With section).

    **CSS additions** (add to the inline style tag):
    ```css
    .performance-graph {
      width: 100%;
      max-width: 800px;
      height: auto;
      margin: 2rem auto;
      display: block;
      border-radius: 8px;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
    }

    .metric-badge {
      display: inline-block;
      padding: 0.25rem 0.75rem;
      background: #e8f5e9;
      color: #2E7D32;
      border-radius: 4px;
      font-weight: 600;
      font-size: 0.9rem;
      margin: 0 0.25rem;
    }

    .comparison-table {
      width: 100%;
      border-collapse: collapse;
      margin: 2rem 0;
      font-size: 0.95rem;
    }

    .comparison-table thead tr {
      background: linear-gradient(135deg, #2E7D32 0%, #1B5E20 100%);
      color: white;
    }

    .comparison-table th,
    .comparison-table td {
      padding: 1rem;
      text-align: left;
      border-bottom: 1px solid #ddd;
    }

    .comparison-table tbody tr:hover {
      background: #f5f5f5;
    }

    .comparison-table td:nth-child(n+3) {
      text-align: center;
    }

    .comparison-table th:nth-child(n+3) {
      text-align: center;
    }

    @media (max-width: 768px) {
      .comparison-table {
        font-size: 0.85rem;
      }
      .comparison-table th,
      .comparison-table td {
        padding: 0.5rem;
      }
    }
    ```

    **Image paths:** Use absolute paths from public root: `src="/projects/cs330/images/d1_comparison.png"` (note: filenames updated to match revised 18-01 output).

    **IMPORTANT:** Read `scripts/cs330_metrics.json` to get the REAL performance values before creating the comparison table. Do not use placeholder data.
  </action>
  <verify>
    Run `npm run build` from project root and confirm no errors.
    Verify file exists: `ls src/pages/cs330/Cs330PerformancePage.jsx`
    Verify file has at least 300 lines: `wc -l src/pages/cs330/Cs330PerformancePage.jsx`
    Grep for all 5 image references: `grep -c 'projects/cs330/images' src/pages/cs330/Cs330PerformancePage.jsx` should return 5.
    Verify metrics JSON was read: comparison table should have real values, not placeholders like "2.45".
  </verify>
  <done>
    Cs330PerformancePage.jsx exists with all 5 embedded graphs showing real CS330 data, algorithm comparison table populated with actual metrics from cs330_metrics.json, metrics overview, key takeaways based on real results, navigation links, and footer. Build succeeds without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register Performance Analysis route in App.jsx</name>
  <files>src/App.jsx</files>
  <action>
    Add to `src/App.jsx`:

    1. Add import at the top with other CS330 imports (after Cs330PathfindingPage):
       ```jsx
       import Cs330PerformancePage from './pages/cs330/Cs330PerformancePage'
       ```

    2. Add route inside the `{/* CS330 Case Study Project */}` section, after the pathfinding route:
       ```jsx
       <Route path="/projects/cs330/docs/performance" element={<Cs330PerformancePage />} />
       ```

    No body class changes needed (cs330-page already handled by the `/projects/cs330/` prefix check).
  </action>
  <verify>
    Run `npm run build` and confirm no errors.
    Verify route exists: `grep -q "Cs330PerformancePage" src/App.jsx && echo "Route registered"`
  </verify>
  <done>
    App.jsx imports Cs330PerformancePage and registers route at /projects/cs330/docs/performance. Build succeeds. Route is active.
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- `/projects/cs330/docs/performance` route exists and renders the Performance Analysis page
- All 5 graph images (d1_comparison.png, d2_comparison.png, d1_d2_combined.png, supply_demand_distribution.png, runtime_comparison.png) referenced in the page
- Algorithm comparison table populated with real metrics from cs330_metrics.json
- Breadcrumbs show: Home > CS330 Case Study > Documentation > Performance Analysis
- All images have descriptive alt text
- Page content references actual performance data, not placeholder values
</verification>

<success_criteria>
- Visitor can navigate to /projects/cs330/docs/performance
- Page shows real D1/D2 graphs from CS330 algorithm simulations
- Algorithm comparison table displays actual measured metrics (not hardcoded estimates)
- Supply-demand scatter plot uses real simulation data
- Runtime comparison shows measured execution times from algorithm runs
- Page uses forest green design system consistent with other CS330 pages
- All images load correctly from /projects/cs330/images/ paths
- Build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/18-performance-analysis-documentation/18-02-SUMMARY.md`
</output>
