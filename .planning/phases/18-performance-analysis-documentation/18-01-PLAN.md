---
phase: 18-performance-analysis-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate_cs330_graphs.py
  - public/projects/cs330/images/d1_time_series.png
  - public/projects/cs330/images/d2_time_series.png
  - public/projects/cs330/images/d1_d2_combined.png
  - public/projects/cs330/images/supply_demand_distribution.png
  - public/projects/cs330/images/runtime_comparison.png
autonomous: true

must_haves:
  truths:
    - "Performance graphs exist as PNG files in public/projects/cs330/images/"
    - "D1 time series graph shows decreasing average distance from T1 to T5"
    - "D2 time series graph shows decreasing maximum distance from T1 to T5"
    - "Combined D1/D2 bar chart compares both metrics side by side"
    - "Supply-demand scatter plot shows rider (red) and driver (green) spatial distribution"
    - "Runtime comparison bar chart shows execution time for each algorithm"
  artifacts:
    - path: "scripts/generate_cs330_graphs.py"
      provides: "Matplotlib graph generation script"
      min_lines: 100
    - path: "public/projects/cs330/images/d1_time_series.png"
      provides: "D1 average distance line graph"
    - path: "public/projects/cs330/images/d2_time_series.png"
      provides: "D2 maximum distance line graph"
    - path: "public/projects/cs330/images/d1_d2_combined.png"
      provides: "Combined D1 vs D2 bar chart"
    - path: "public/projects/cs330/images/supply_demand_distribution.png"
      provides: "Supply-demand spatial scatter plot"
    - path: "public/projects/cs330/images/runtime_comparison.png"
      provides: "Algorithm runtime bar chart"
  key_links:
    - from: "scripts/generate_cs330_graphs.py"
      to: "public/projects/cs330/images/"
      via: "plt.savefig output path"
      pattern: "savefig.*public/projects/cs330/images"
---

<objective>
Generate matplotlib performance analysis graphs for CS330 documentation.

Purpose: Create 5 high-quality PNG graphs (D1 time series, D2 time series, D1/D2 combined, supply-demand scatter, runtime comparison) that will be embedded in the Performance Analysis React page. Graphs use portfolio forest green (#2E7D32) color scheme and 150 DPI for retina display quality.

Output: 5 PNG files in `public/projects/cs330/images/` and a reusable Python generation script in `scripts/`.
</objective>

<execution_context>
@/Users/orases/.claude/get-shit-done/workflows/execute-plan.md
@/Users/orases/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-performance-analysis-documentation/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install matplotlib and create graph generation script</name>
  <files>scripts/generate_cs330_graphs.py</files>
  <action>
    First, install matplotlib and numpy via pip3:
    ```
    pip3 install matplotlib numpy
    ```

    Then create the directory structure:
    ```
    mkdir -p public/projects/cs330/images
    mkdir -p scripts
    ```

    Create `scripts/generate_cs330_graphs.py` containing 5 graph generation functions using matplotlib. The script must:

    1. Use consistent styling across all graphs:
       - `plt.style.use('seaborn-v0_8-whitegrid')` (versioned style name, not deprecated)
       - Portfolio colors: PORTFOLIO_GREEN = '#2E7D32', PORTFOLIO_DARK_GREEN = '#1B5E20', DEMAND_RED = '#d32f2f'
       - Font sizes: figure 10x6, axes label 12, title 14, legend 10
       - All graphs saved at dpi=150 with facecolor='white' and bbox_inches='tight'

    2. Algorithm data (use these exact values for consistency with documentation):
       - Algorithms: T1 (Brute Force), T2 (Sorted), T3 (Grid), T4 (KD-Tree), T5 (KD+Dijkstra)
       - D1 values (avg distance km): [2.45, 2.38, 1.95, 1.82, 1.65]
       - D2 values (max distance km): [5.82, 5.65, 4.20, 3.95, 3.45]
       - Runtimes (ms for 1000 riders/drivers): [1250, 980, 450, 180, 320]

    3. Five generation functions:
       - `generate_d1_graph()`: Line graph with circle markers, annotated value labels in white boxes with green border. Title: "D1 Performance Evolution: Algorithm Comparison". Save to `public/projects/cs330/images/d1_time_series.png`
       - `generate_d2_graph()`: Line graph with square markers using PORTFOLIO_DARK_GREEN. Title: "D2 Performance Evolution: Algorithm Comparison". Save to `public/projects/cs330/images/d2_time_series.png`
       - `generate_combined_graph()`: Grouped bar chart with D1 bars in PORTFOLIO_GREEN and D2 bars in PORTFOLIO_DARK_GREEN. Title: "D1 vs D2 Performance Comparison". Save to `public/projects/cs330/images/d1_d2_combined.png`
       - `generate_supply_demand_scatter()`: Scatter plot with 100 riders (red circles, s=60) and 50 drivers (green triangles, s=120) using np.random.seed(42) for NYC-area coordinates (lat 40.70-40.80, lon -74.02 to -73.92). Title: "Supply-Demand Spatial Distribution". Save to `public/projects/cs330/images/supply_demand_distribution.png`
       - `generate_runtime_graph()`: Bar chart with green for runtimes under 500ms and orange (#f57c00) for runtimes above. Value labels on top of each bar. Title: "Algorithm Runtime Comparison (1000 riders/drivers)". Save to `public/projects/cs330/images/runtime_comparison.png`

    4. Main block that calls all 5 functions and prints status.

    NOTE: If `seaborn-v0_8-whitegrid` style is not available (varies by matplotlib version), fall back to trying `seaborn-whitegrid`, then use default style with manual grid settings. Wrap the style selection in a try/except block.

    After creating the script, run it:
    ```
    cd /Users/orases/Aaron/website && python3 scripts/generate_cs330_graphs.py
    ```
  </action>
  <verify>
    Run `ls -la public/projects/cs330/images/` and confirm all 5 PNG files exist with non-zero file sizes.
    Run `python3 scripts/generate_cs330_graphs.py` completes without errors.
  </verify>
  <done>
    5 PNG files exist in public/projects/cs330/images/: d1_time_series.png, d2_time_series.png, d1_d2_combined.png, supply_demand_distribution.png, runtime_comparison.png. All files are non-zero size and generated at 150 DPI.
  </done>
</task>

</tasks>

<verification>
- `ls public/projects/cs330/images/*.png` lists exactly 5 files
- Each PNG file is at least 10KB (non-trivial content)
- `python3 scripts/generate_cs330_graphs.py` runs without errors
</verification>

<success_criteria>
- 5 performance graph PNGs exist in public/projects/cs330/images/
- Graphs use consistent forest green color scheme
- Graphs saved at 150 DPI for retina quality
- Generation script is reusable (can be re-run to regenerate)
</success_criteria>

<output>
After completion, create `.planning/phases/18-performance-analysis-documentation/18-01-SUMMARY.md`
</output>
