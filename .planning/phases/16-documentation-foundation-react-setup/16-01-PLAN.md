---
phase: 16-documentation-foundation-react-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/cs330/Cs330DocsLanding.jsx
  - src/App.jsx
autonomous: true

must_haves:
  truths:
    - "Visitor can navigate to /projects/cs330/docs and see a CS330 landing page"
    - "Landing page uses forest green design system (#2E7D32 gradient header, #2c3e50 headings, white sections)"
    - "Landing page has breadcrumb navigation (Home > CS330 Case Study > Documentation)"
    - "Landing page has doc cards linking to future documentation pages (algorithm, kdtree, pathfinding, performance, bonus)"
    - "CS330 routes are registered in App.jsx with body class handling"
    - "Page uses inline CSS in style tags matching CpuDocsLanding.jsx pattern"
  artifacts:
    - path: "src/pages/cs330/Cs330DocsLanding.jsx"
      provides: "CS330 documentation landing page"
      min_lines: 150
    - path: "src/App.jsx"
      provides: "Route registration and body class for CS330"
      contains: "/projects/cs330/docs"
  key_links:
    - from: "src/App.jsx"
      to: "src/pages/cs330/Cs330DocsLanding.jsx"
      via: "Route element import and path registration"
      pattern: "Route path=.*cs330.*element=.*Cs330DocsLanding"
    - from: "src/pages/cs330/Cs330DocsLanding.jsx"
      to: "src/components/shared/Breadcrumbs.jsx"
      via: "import and render with items array"
      pattern: "import Breadcrumbs from.*Breadcrumbs"
    - from: "src/pages/cs330/Cs330DocsLanding.jsx"
      to: "/projects/cs330/docs/*"
      via: "React Router Link components in doc cards"
      pattern: "Link to=.*/projects/cs330/docs/"
---

<objective>
Create the CS330 Case Study documentation landing page and register routes in App.jsx.

Purpose: Establish the foundation for CS330 documentation by creating the entry point page that visitors will see at `/projects/cs330/docs`, following the exact same patterns used by the CPU documentation (CpuDocsLanding.jsx).

Output: Working landing page accessible via React Router, with forest green design system, breadcrumbs, doc cards for future pages, and a footer.
</objective>

<execution_context>
@/Users/orases/.claude/get-shit-done/workflows/execute-plan.md
@/Users/orases/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-documentation-foundation-react-setup/16-RESEARCH.md

# Template to follow exactly:
@src/pages/cpu/CpuDocsLanding.jsx
@src/App.jsx
@src/components/shared/Breadcrumbs.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CS330 landing page component</name>
  <files>src/pages/cs330/Cs330DocsLanding.jsx</files>
  <action>
Create `src/pages/cs330/Cs330DocsLanding.jsx` by adapting the CpuDocsLanding.jsx pattern.

Structure (follow CpuDocsLanding.jsx exactly for layout, CSS, and component patterns):

1. **Imports:** React, Link from react-router-dom, Breadcrumbs from `../../components/shared/Breadcrumbs`

2. **Breadcrumbs:** items array with:
   - { label: 'Home', href: '/' }
   - { label: 'CS330 Case Study', href: '/projects/cs330/docs' }
   - { label: 'Documentation' }

3. **Inline CSS in style tag:** Copy the FULL CSS from CpuDocsLanding.jsx (landing-header, container, section, doc-cards, doc-card, quick-links, quick-link, footer, and all media queries). This is NOT optional -- copy all styles to maintain design consistency. Remove CPU-specific classes (pipeline-diagram, pipeline-step, pipeline-arrow, gallery-grid, gallery-item, code-snippet) since CS330 won't use those.

4. **Header:** gradient header with:
   - h1: "CS330 Case Study"
   - p: "Ride-Sharing Matching Algorithm Design"

5. **About section:** White card explaining the project:
   - What this is: Algorithm design case study from CS330 - Design and Analysis of Algorithms
   - Problem: Ride-sharing platform matching riders with nearby drivers efficiently
   - Approach: Progressive algorithm evolution from naive (T1) to optimized (T5) with spatial indexing and graph-based pathfinding
   - Key highlights as ul: Algorithm evolution (T1-T5 progressive optimization), KD-Tree spatial indexing for O(log n) nearest-neighbor queries, Dijkstra's algorithm for road network pathfinding, Performance analysis with D1/D2 metrics, Bonus optimizations (load balancing, traffic modeling, caching)

6. **Documentation cards section:** doc-cards grid with Link components to future pages:
   - "Algorithm Evolution" -> /projects/cs330/docs/algorithm, description: "T1-T5 progressive optimization from brute force to spatial indexing"
   - "KD-Tree Spatial Indexing" -> /projects/cs330/docs/kdtree, description: "Efficient nearest-neighbor search with balanced tree partitioning"
   - "Pathfinding" -> /projects/cs330/docs/pathfinding, description: "Dijkstra's algorithm on weighted road networks"
   - "Performance Analysis" -> /projects/cs330/docs/performance, description: "D1/D2 metrics, comparison graphs, and algorithm benchmarks"
   - "Bonus Algorithms" -> /projects/cs330/docs/bonus, description: "Load balancing, traffic-aware routing, predictive caching"

7. **Quick links section:** White card with quick-link buttons:
   - "View Source on GitHub" -> https://github.com/AaronDiefes/cs330-case-study (external link, target="_blank" rel="noopener noreferrer", use `<a>` tag not Link)
   - "Back to Portfolio" -> / (use Link)

8. **Footer:** Copy the EXACT footer structure from CpuDocsLanding.jsx (4-column grid with inline styles), updating content:
   - Column 1 (About): "Aaron Diefes" heading, description about algorithm design and software engineering
   - Column 2 (Navigation): Home, CS330 Documentation (/projects/cs330/docs), Source Code Viewer (/admin) -- use Link components
   - Column 3 (Resources): GitHub Profile, CS330 Case Study Repo, Portfolio Repo -- use `<a>` tags with target="_blank"
   - Column 4 (Built With): Python, NetworkX, KD-Trees, Matplotlib
   - Bottom bar: copyright 2026

IMPORTANT: This is a documentation-only project. There is NO demo page, so do NOT include a "Try Demo" link (unlike the CPU landing which has "Try Interactive Simulator"). The quick links should have GitHub and Back to Portfolio only.

Export: `export default Cs330DocsLanding`
  </action>
  <verify>
Run `npm run build` from project root. Build should complete without errors. Verify the file exists and has correct imports:
- `grep -r "Breadcrumbs" src/pages/cs330/Cs330DocsLanding.jsx` should show import
- `grep -r "Link" src/pages/cs330/Cs330DocsLanding.jsx` should show react-router-dom import
- `grep "#2E7D32" src/pages/cs330/Cs330DocsLanding.jsx` should show forest green usage
  </verify>
  <done>
Cs330DocsLanding.jsx exists in src/pages/cs330/ with: forest green gradient header, breadcrumbs, project description, 5 doc cards linking to future pages, quick links to GitHub and portfolio, and 4-column footer. All CSS is inline in a style tag. Component uses React Router Link for internal links and `<a>` for external links.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register CS330 routes and body class in App.jsx</name>
  <files>src/App.jsx</files>
  <action>
Update `src/App.jsx` to add CS330 routing and body class management.

1. **Add import at top** (after the CPU page imports section):
```jsx
// CS330 Case Study pages
import Cs330DocsLanding from './pages/cs330/Cs330DocsLanding'
```

2. **Add route** (after the Graphics Engine routes block, before closing `</Routes>`):
```jsx
{/* CS330 Case Study Project */}
<Route path="/projects/cs330/docs" element={<Cs330DocsLanding />} />
```

Note: Only register the landing page route for now. Future phase plans will add the individual doc page routes (algorithm, kdtree, pathfinding, performance, bonus) as those components are created.

3. **Update body class handling** in the AppContent useEffect:
- Add `'cs330-page'` to the `classList.remove()` call
- Add condition after the graphics-engine check:
```jsx
} else if (location.pathname.startsWith('/projects/cs330/')) {
  document.body.classList.add('cs330-page')
}
```

The full classList.remove should be:
```jsx
document.body.classList.remove(
  'home-page',
  'cpu-page',
  'graphics-page',
  'cs330-page',
  'docs-page',
  'admin-page'
)
```
  </action>
  <verify>
Run `npm run build` from project root -- should succeed with no errors. Then run `npm run dev` briefly and verify:
1. Navigate to http://localhost:5173/projects/cs330/docs in browser or use `curl -s http://localhost:5173/projects/cs330/docs | head -20` to confirm the page is served
2. Check that `grep "cs330" src/App.jsx` shows the import, route, and body class lines
  </verify>
  <done>
App.jsx has: Cs330DocsLanding import, Route for /projects/cs330/docs, and cs330-page body class handling. `npm run build` succeeds. Navigating to /projects/cs330/docs renders the CS330 landing page.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `src/pages/cs330/Cs330DocsLanding.jsx` exists with 150+ lines
3. `src/App.jsx` contains Route for `/projects/cs330/docs`
4. Landing page renders with forest green header, breadcrumbs, doc cards, and footer
5. Doc card Links point to future pages (/projects/cs330/docs/algorithm, etc.)
6. All internal links use React Router Link, all external links use `<a>` with target="_blank"
</verification>

<success_criteria>
- Visitor can navigate to /projects/cs330/docs and see a complete CS330 Case Study landing page
- Page matches forest green design system used by CPU and Graphics Engine docs
- Breadcrumbs show: Home > CS330 Case Study > Documentation
- 5 doc cards link to future documentation pages
- Footer has 4-column layout matching portfolio style
- Build passes with no errors or warnings
</success_criteria>

<output>
After completion, create `.planning/phases/16-documentation-foundation-react-setup/16-01-SUMMARY.md`
</output>
