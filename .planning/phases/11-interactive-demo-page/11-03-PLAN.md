---
phase: 11-interactive-demo-page
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - cpu-simulator/index.html
  - cpu-simulator/src/visualization/cpu-visualizer.js
autonomous: false

must_haves:
  truths:
    - "Demo page loads all Phase 9, 10, and 11 modules and renders functional visualization"
    - "User can step forward and backward through animation using buttons or keyboard (Left/Right arrows)"
    - "User can play/pause animation with Space key or button click"
    - "User can reset animation with button or R key"
    - "User can switch programs via dropdown and animation resets to new program"
    - "Memory visualization updates as animation progresses (shows non-zero addresses)"
    - "Current instruction display updates with field breakdown on each frame"
    - "Instruction list highlights current instruction and supports click-to-jump"
    - "All controls are keyboard-accessible"
  artifacts:
    - path: "cpu-simulator/index.html"
      provides: "Complete interactive CPU simulator demo page"
      contains: "cpu-simulator"
    - path: "cpu-simulator/src/visualization/cpu-visualizer.js"
      provides: "Updated coordinator with MemoryView and InstructionView"
      contains: "MemoryView"
  key_links:
    - from: "cpu-simulator/index.html"
      to: "All Phase 9/10/11 JS modules"
      via: "Script tags load modules in dependency order"
      pattern: "<script src"
    - from: "cpu-simulator/index.html"
      to: "Keyboard event handling"
      via: "keydown listener delegates to AnimationEngine methods"
      pattern: "keydown|e\\.code.*Space|ArrowLeft|ArrowRight"
    - from: "cpu-simulator/src/visualization/cpu-visualizer.js"
      to: "MemoryView and InstructionView"
      via: "constructor creates instances, render() delegates to them"
      pattern: "new MemoryView|new InstructionView|memoryView\\.render|instructionView\\.render"
    - from: "cpu-simulator/index.html"
      to: "ProgramSelector.onProgramLoad callback"
      via: "Callback wires InstructionList.loadProgram and initial render"
      pattern: "onProgramLoad|instructionList\\.loadProgram"
---

<objective>
Assemble the complete interactive demo page by creating cpu-simulator/index.html, updating CPUVisualizer to include MemoryView and InstructionView, and wiring all components together with keyboard shortcuts.

Purpose: This is the culmination of Phase 11 - a fully functional demo page where users can interactively explore CPU execution through step-by-step animation, program switching, and multiple visualization panels.

Output: Complete demo page (index.html) and updated CPUVisualizer coordinator.
</objective>

<execution_context>
@/Users/orases/.claude/get-shit-done/workflows/execute-plan.md
@/Users/orases/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/11-interactive-demo-page/11-01-SUMMARY.md
@.planning/phases/11-interactive-demo-page/11-02-SUMMARY.md
@.planning/phases/11-interactive-demo-page/11-RESEARCH.md

@cpu-simulator/src/visualization/cpu-visualizer.js
@cpu-simulator/src/visualization/visualization.css
@cpu-simulator/src/animation/animation-engine.js
@graphics-demo.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CPUVisualizer and create demo page</name>
  <files>
    cpu-simulator/src/visualization/cpu-visualizer.js
    cpu-simulator/index.html
  </files>
  <action>
**Update CPUVisualizer (cpu-visualizer.js):**
Add MemoryView and InstructionView as child views to the coordinator. Update the existing file:

1. Update _createDOM() to add new sections:
```html
<section class="diagram-section">
  <div data-block-diagram></div>
</section>
<section class="info-section">
  <div class="info-panels">
    <section class="registers-section">
      <h2>Registers</h2>
      <div class="register-grid" data-register-view></div>
    </section>
    <section class="memory-section">
      <div data-memory-view></div>
    </section>
    <section class="instruction-detail-section">
      <div data-instruction-view></div>
    </section>
  </div>
</section>
```

2. Add MemoryView and InstructionView initialization in constructor:
```javascript
this.memoryView = new MemoryView(
  this.container.querySelector('[data-memory-view]')
);
this.instructionView = new InstructionView(
  this.container.querySelector('[data-instruction-view]')
);
```

3. Update render(state) to delegate to all 4 views:
```javascript
render(state) {
  this.blockDiagramView.render(state);
  this.registerView.render(state);
  this.memoryView.render(state);
  this.instructionView.render(state);
}
```

4. Update destroy() to null all 4 view references.

**Create Demo Page (cpu-simulator/index.html):**
Create the main demo page following the graphics-demo.html structure pattern (same design system):

1. HTML head:
   - Title: "CPU Pipeline Simulator - Aaron Diefes"
   - Link design-system.css (path: ../docs/assets/css/design-system.css)
   - Link nav.css (path: ../docs/assets/css/nav.css)
   - Link breadcrumbs.css (path: ../docs/assets/css/breadcrumbs.css)
   - Link visualization.css (path: src/visualization/visualization.css)
   - Inline `<style>` for page-specific layout (see layout below)

2. Page layout structure:
```html
<body>
  <!-- Site navigation (same pattern as graphics-demo.html) -->
  <nav class="site-nav">...</nav>

  <div class="container">
    <header>
      <h1>CPU Pipeline Simulator</h1>
      <p>Interactive 5-stage RISC pipeline visualization with step-through execution</p>
    </header>

    <main class="demo-layout">
      <!-- Left sidebar: controls -->
      <aside class="sidebar">
        <div id="program-selector-container"></div>
        <div id="control-panel-container"></div>
        <div id="instruction-list-container"></div>
      </aside>

      <!-- Main content: visualization -->
      <div class="visualization-area">
        <div id="cpu-viz-container"></div>
      </div>
    </main>
  </div>

  <!-- Scripts in dependency order -->
</body>
```

3. CSS layout (inline styles):
   - body: forest green gradient background (same as graphics-demo.html)
   - .container: max-width 1400px, white background, border-radius, shadow
   - header: dark gradient (#2c3e50 to #34495e), white text, centered
   - .demo-layout: CSS Grid, grid-template-columns: 300px 1fr
   - .sidebar: padding, light background (#f8f9fa), border-radius, overflow-y: auto, max-height: calc(100vh - 200px)
   - .visualization-area: padding, min-width: 0 (for SVG responsive sizing)
   - Control panel button styles: forest green buttons matching design system
   - .control-panel: flex-wrap layout for controls
   - .btn-reset, .btn-step-back, .btn-play-pause, .btn-step-forward: styled buttons with hover states
   - .program-selector: margin-bottom, styled select
   - Responsive: at max-width 1024px, switch to single-column layout (grid-template-columns: 1fr)
   - Keyboard shortcuts help text at bottom of sidebar

4. Script loading (in order):
```html
<!-- Phase 9: Core -->
<script src="src/core/cpu-state.js"></script>
<script src="src/core/instruction-set.js"></script>
<script src="src/core/sequence-generator.js"></script>

<!-- Phase 9: Animation -->
<script src="src/animation/timing-controller.js"></script>
<script src="src/animation/animation-engine.js"></script>

<!-- Phase 9: Programs -->
<script src="src/programs/basic-instructions.js"></script>
<script src="src/programs/fibonacci.js"></script>

<!-- Phase 10: Visualization -->
<script src="src/visualization/block-diagram-view.js"></script>
<script src="src/visualization/register-view.js"></script>
<script src="src/visualization/memory-view.js"></script>
<script src="src/visualization/instruction-view.js"></script>
<script src="src/visualization/cpu-visualizer.js"></script>

<!-- Phase 11: UI Controls -->
<script src="src/ui/instruction-list.js"></script>
<script src="src/ui/control-panel.js"></script>
<script src="src/ui/program-selector.js"></script>

<!-- Phase 11: Page initialization -->
<script src="src/ui/demo-init.js"></script>
```

Wait -- actually, keep initialization inline in index.html instead of creating a separate demo-init.js. Put the init script in an inline `<script>` at the bottom:

5. Inline initialization script:
```javascript
(function() {
  'use strict';

  // Create animation engine
  const engine = new AnimationEngine();

  // Create visualization
  const visualizer = new CPUVisualizer(
    document.getElementById('cpu-viz-container')
  );

  // Create instruction list
  const instructionList = new InstructionList(
    document.getElementById('instruction-list-container'),
    engine
  );

  // Create control panel
  const controlPanel = new ControlPanel(
    document.getElementById('control-panel-container'),
    engine
  );

  // Create program selector (loads default program)
  const programSelector = new ProgramSelector(
    document.getElementById('program-selector-container'),
    engine,
    {
      defaultProgram: 'basic',
      onProgramLoad: function(program, result) {
        // Wire instruction list
        instructionList.loadProgram(program, result);
        // Render initial state
        visualizer.render(result.frames[0]);
      }
    }
  );

  // ========== KEYBOARD SHORTCUTS ==========
  document.addEventListener('keydown', function(e) {
    // Don't interfere with input fields
    if (e.target.matches('input, textarea, select')) {
      return;
    }

    switch (e.code) {
      case 'Space':
        e.preventDefault();
        engine.togglePlayPause();
        break;
      case 'ArrowLeft':
        e.preventDefault();
        engine.stepBackward();
        break;
      case 'ArrowRight':
        e.preventDefault();
        engine.stepForward();
        break;
      case 'KeyR':
        if (!e.ctrlKey && !e.metaKey) {
          e.preventDefault();
          engine.reset();
        }
        break;
    }
  });
})();
```

6. Keyboard shortcuts help section in sidebar:
```html
<div class="keyboard-shortcuts">
  <h3>Keyboard Shortcuts</h3>
  <dl>
    <dt><kbd>Space</kbd></dt><dd>Play / Pause</dd>
    <dt><kbd>&larr;</kbd></dt><dd>Step Back</dd>
    <dt><kbd>&rarr;</kbd></dt><dd>Step Forward</dd>
    <dt><kbd>R</kbd></dt><dd>Reset</dd>
  </dl>
</div>
```

7. Include site navigation following nav.css/nav.js pattern from graphics-demo.html. Load nav.js script: `<script src="../docs/assets/js/nav.js"></script>`

**Important notes:**
- Follow graphics-demo.html structure closely for consistent portfolio design
- Do NOT load PipelineView or ExecutionView scripts (removed in Phase 10-04)
- Script loading order matters: dependencies before dependents
- Keyboard shortcuts check e.target to avoid interfering with select elements
- Ctrl+R / Cmd+R not captured (allows browser reload)
  </action>
  <verify>
Verify files and structure:
- `[ -f cpu-simulator/index.html ] && echo "FOUND"`
- grep for "CPUVisualizer\|AnimationEngine\|ControlPanel\|ProgramSelector\|InstructionList" in index.html
- grep for "new MemoryView\|new InstructionView" in cpu-visualizer.js
- grep for "memoryView.render\|instructionView.render" in cpu-visualizer.js
- grep for "keydown\|ArrowLeft\|ArrowRight\|Space" in index.html
- grep for "design-system.css\|nav.css\|visualization.css" in index.html
- Count script tags in index.html (should be ~16+)
- grep for "memory-view.js\|instruction-view.js\|instruction-list.js" in index.html
  </verify>
  <done>
CPUVisualizer updated with 4 child views: BlockDiagramView, RegisterView, MemoryView, InstructionView.
Demo page loads all modules in correct order.
All components wired together: ProgramSelector -> SequenceGenerator -> AnimationEngine -> CPUVisualizer.
Keyboard shortcuts (Space, Left/Right arrows, R) work without interfering with inputs.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify interactive demo page</name>
  <files>cpu-simulator/index.html</files>
  <action>
Human verification of the complete interactive demo page.

What was built: Complete interactive CPU simulator demo page with all visualization components, playback controls, program switching, and keyboard navigation.

How to verify:
1. Open cpu-simulator/index.html in Chrome/Firefox
2. Verify initial state: Block diagram visible, registers showing, "Basic Instructions Walkthrough" selected
3. Click "Step Fwd" button - verify:
   - Block diagram highlights IF stage components
   - Register grid updates (may not change on first step)
   - Instruction list highlights first instruction
   - Current instruction view shows instruction details
   - Frame counter updates (e.g., "Frame 1 of 45")
4. Click "Step Fwd" several more times - verify memory view shows values after SW instruction executes
5. Click "Step Back" - verify frame goes backward
6. Click "Play" - verify auto-advancing; click "Pause" to stop
7. Change speed to "4x" - click "Play" - verify faster playback
8. Click "Reset" - verify returns to Frame 0
9. Click on an instruction in the instruction list - verify animation jumps to that instruction's frame
10. Switch program to "Fibonacci Sequence" - verify new program loads, instruction list updates, animation resets
11. Test keyboard: press Right Arrow (step forward), Left Arrow (step back), Space (play/pause), R (reset)
12. Verify responsive layout: resize window below 1024px and verify single-column layout

Resume signal: Type "approved" or describe any issues found
  </action>
  <verify>User confirms all 12 verification steps pass</verify>
  <done>Interactive demo page visually verified and approved by user</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. cpu-simulator/index.html exists and renders functional demo page
2. CPUVisualizer manages 4 child views (block diagram, registers, memory, instruction)
3. All 9 Phase 11 requirements satisfied: DEMO-01 through DEMO-07, VIZ-04, VIZ-05
4. Keyboard shortcuts work: Space, Left/Right arrows, R
5. Program selector switches between Basic and Fibonacci
6. Page follows portfolio design system (forest green, consistent navigation)
7. Responsive design works at mobile breakpoints
</verification>

<success_criteria>
- Demo page fully functional with all controls
- SVG block diagram, registers, memory, and instruction views all update on frame changes
- Step forward/backward, play/pause/reset, speed control, and program switching all work
- Click-to-jump on instruction list navigates to correct frame
- Keyboard shortcuts do not interfere with browser defaults or input fields
- Page visually consistent with existing portfolio design
</success_criteria>

<output>
After completion, create `.planning/phases/11-interactive-demo-page/11-03-SUMMARY.md`
</output>
