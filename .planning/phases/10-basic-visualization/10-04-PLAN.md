---
phase: 10-basic-visualization
plan: 04
type: execute
wave: 2
depends_on: ["10-03"]
files_modified:
  - "cpu-simulator/src/visualization/cpu-visualizer.js"
  - "cpu-simulator/test-visualization.html"
autonomous: false

must_haves:
  truths:
    - "CPUVisualizer creates BlockDiagramView as the primary visualization instead of PipelineView"
    - "CPUVisualizer no longer creates ExecutionView (cycle/instruction counter removed per user decision)"
    - "PipelineView is no longer instantiated by CPUVisualizer"
    - "Block diagram and register grid both render correctly when cpu:framechange event fires"
    - "Stepping forward through frames highlights active components in the SVG diagram"
    - "Pipeline register instruction text updates as frames advance"
    - "Register grid still shows 32 registers with change highlighting"
    - "Test page validates new BlockDiagramView integration alongside existing RegisterView tests"
  artifacts:
    - path: "cpu-simulator/src/visualization/cpu-visualizer.js"
      provides: "Updated coordinator using BlockDiagramView + RegisterView (no PipelineView, no ExecutionView)"
      exports: ["CPUVisualizer"]
    - path: "cpu-simulator/test-visualization.html"
      provides: "Updated test page with BlockDiagramView tests and live demo"
  key_links:
    - from: "cpu-visualizer.js"
      to: "block-diagram-view.js"
      via: "new BlockDiagramView() and render(state) delegation"
      pattern: "BlockDiagramView"
    - from: "cpu-visualizer.js"
      to: "register-view.js"
      via: "new RegisterView() and render(state) delegation (preserved)"
      pattern: "RegisterView"
    - from: "cpu-visualizer.js"
      to: "animation-engine.js"
      via: "Listens to cpu:framechange CustomEvent on window"
      pattern: "cpu:framechange"
    - from: "test-visualization.html"
      to: "block-diagram-view.js"
      via: "Script tag loads BlockDiagramView for testing"
      pattern: "block-diagram-view\\.js"
---

<objective>
Integrate the SVG block diagram into the CPUVisualizer coordinator and update the test page to validate the new visualization approach.

Purpose: Wire the new BlockDiagramView into the existing event-driven architecture, replacing PipelineView and ExecutionView. The test page must validate that the block diagram renders, highlights active components, and displays instructions in pipeline registers - the core user-facing behaviors.

Output: Updated `cpu-visualizer.js` (coordinator) and updated `test-visualization.html` (integration tests + live demo)
</objective>

<execution_context>
@/Users/orases/.claude/get-shit-done/workflows/execute-plan.md
@/Users/orases/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-basic-visualization/10-CONTEXT.md
@.planning/phases/10-basic-visualization/10-RESEARCH.md
@.planning/phases/10-basic-visualization/10-03-SUMMARY.md
@cpu-simulator/src/visualization/cpu-visualizer.js
@cpu-simulator/src/visualization/block-diagram-view.js
@cpu-simulator/src/visualization/register-view.js
@cpu-simulator/test-visualization.html
@cpu-simulator/src/core/cpu-state.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CPUVisualizer coordinator for block diagram</name>
  <files>cpu-simulator/src/visualization/cpu-visualizer.js</files>
  <action>
Rewrite `cpu-visualizer.js` to use the new BlockDiagramView instead of PipelineView, and remove ExecutionView (per user decision to remove cycle/instruction counter text).

**Changes to constructor:**
- Remove `this.pipelineView = new PipelineView(...)`
- Remove `this.executionView = new ExecutionView(...)`
- Add `this.blockDiagramView = new BlockDiagramView(...)` targeting a `[data-block-diagram]` container
- Keep `this.registerView = new RegisterView(...)` targeting `[data-register-view]` container

**Changes to _createDOM():**
Replace the existing innerHTML structure. The new structure should be:
```html
<section class="diagram-section">
  <div data-block-diagram></div>
</section>
<section class="registers-section">
  <h2>Registers</h2>
  <div class="register-grid" data-register-view></div>
</section>
```

Key changes:
- Remove the `<section class="pipeline-section">` with `<h2>Pipeline Stages</h2>` and `<div class="pipeline-stages" data-pipeline-view>`
- Remove the `<aside class="data-section">` wrapper
- Remove the `<section class="execution-section">` with cycle/instruction counters
- Add `<section class="diagram-section">` with `<div data-block-diagram>` for BlockDiagramView
- Keep `<section class="registers-section">` with register grid (promoted to top-level, no longer inside aside)
- Remove `aria-live="polite"` from execution view (it no longer exists)

**Changes to render(state):**
```javascript
render(state) {
  this.blockDiagramView.render(state);
  this.registerView.render(state);
}
```
Remove `this.pipelineView.render(state)` and `this.executionView.render(state)`.

**Changes to destroy():**
- Null `this.blockDiagramView` instead of `this.pipelineView` and `this.executionView`
- Keep `this.registerView` cleanup

**Keep all other patterns:**
- IIFE wrapper with `window.CPUVisualizer` export
- Event listener for `cpu:framechange` on window
- `_handleFrameChange(event)` checking `event.detail.state`
- Bind pattern for `_handleFrameChange`
  </action>
  <verify>
Check `cpu-visualizer.js`:
- Contains `new BlockDiagramView` (not `new PipelineView`)
- Does NOT contain `PipelineView` anywhere
- Does NOT contain `ExecutionView` anywhere
- Does NOT contain `data-pipeline-view` or `data-execution-view`
- Contains `data-block-diagram` attribute in DOM structure
- Contains `data-register-view` attribute (preserved)
- `render()` method calls `this.blockDiagramView.render(state)` and `this.registerView.render(state)`
- Still listens to `cpu:framechange` event
- Still uses IIFE + `window.CPUVisualizer` export
  </verify>
  <done>
CPUVisualizer coordinator creates BlockDiagramView + RegisterView (no PipelineView, no ExecutionView). DOM structure has diagram section and registers section. Event-driven rendering delegates to both views.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update integration test page for block diagram</name>
  <files>cpu-simulator/test-visualization.html</files>
  <action>
Rewrite `test-visualization.html` to test the new BlockDiagramView-based visualization. The page should follow the existing dual-purpose pattern: automated tests at top, live demo at bottom.

**Script tags:** Update the script loading section:
- Keep: cpu-state.js, instruction-set.js, sequence-generator.js, animation-engine.js, timing-controller.js
- Keep: register-view.js
- Add: block-diagram-view.js
- Remove: pipeline-view.js (no longer used by coordinator)
- Remove: execution-view.js (no longer used by coordinator)
- Keep: cpu-visualizer.js
- Keep: basic-instructions.js, fibonacci.js
- Load design-system.css and visualization.css

**Automated Test Suite - Replace PipelineView and ExecutionView tests with BlockDiagramView tests:**

**BlockDiagramView Tests (7 tests):**
1. "Creates SVG element with viewBox" - Instantiate BlockDiagramView in a test container. Assert the container has an `<svg>` child with a `viewBox` attribute.
2. "Renders major hardware components" - Check for `[data-component="IMEM"]`, `[data-component="REGFILE"]`, `[data-component="ALU"]`, `[data-component="DMEM"]` elements inside the SVG.
3. "Renders pipeline registers" - Check for `[data-pipeline-reg="FD"]`, `[data-pipeline-reg="DX"]`, `[data-pipeline-reg="XM"]`, `[data-pipeline-reg="MW"]` elements.
4. "Renders multiplexers" - Check for at least 2 elements with `[data-mux]` attribute.
5. "Highlights active components on render" - Create a CPUState with `pipeline.EX.active = true`, call `render(state)`. Assert `[data-component="ALU"]` has class `active`.
6. "Updates pipeline register instruction display" - Create a CPUState with `pipeline.IF.active = true` and `pipeline.IF.instruction` set to an ADD instruction. Call render. Check `[data-instruction-display="FD"]` textContent is not 'NOP' (should show instruction text).
7. "Shows NOP when stage is inactive" - Create a CPUState with all stages inactive. Call render. Check all `[data-instruction-display]` elements show 'NOP'.

**RegisterView Tests (keep existing 5 tests):**
1. Creates 32 register cells with data-register attributes
2. Shows hex format values
3. Highlights changed registers
4. Removes highlight on next render
5. Register $0 cell exists

**CPUVisualizer Integration Tests (5 tests):**
1. "Creates DOM with block diagram and register sections" - Instantiate CPUVisualizer. Assert `[data-block-diagram]` and `[data-register-view]` exist in container.
2. "Does NOT create pipeline-view or execution-view" - Assert `[data-pipeline-view]` and `[data-execution-view]` do NOT exist.
3. "Responds to cpu:framechange event" - Dispatch synthetic event with a CPUState, verify SVG has active components.
4. "Full program playback" - Load BASIC_PROGRAM, generate sequence, step forward 6+ times, verify SVG has at least one `.active` component and register values have updated.
5. "destroy() cleans up" - Call destroy(), verify container is empty, verify events don't throw.

**Total: ~17 tests** (7 BlockDiagram + 5 Register + 5 Integration)

**Live Demo Section (bottom of page):**
- Program selector dropdown (Basic Instructions / Fibonacci)
- Step Forward, Step Back, Reset buttons
- Frame counter display
- Full CPUVisualizer instance rendering into a demo container
- The demo container should be wide enough to display the block diagram comfortably (min-width: 900px or full-width)
- Style the demo section with a border, padding, and the design system colors

**Test infrastructure:**
- Reuse the simple `test()`, `assert()`, `assertEquals()` framework from the existing test page
- Each test creates a fresh container element to avoid cross-test pollution
- Results grouped by component (BlockDiagramView, RegisterView, CPUVisualizer)
- Green/red pass/fail with error details
- Summary count at top

**CSS for test page:**
- Include both design-system.css and visualization.css
- Add minimal test-specific styles inline (test result colors, layout)
  </action>
  <verify>
Open test-visualization.html in a browser (or check file contents):
- Page loads without console errors
- Script tags include block-diagram-view.js (not pipeline-view.js, not execution-view.js)
- Contains BlockDiagramView test section with 7 tests
- Contains RegisterView test section with 5 tests
- Contains CPUVisualizer test section with 5 tests
- Live demo section has program selector, step controls, and visualization container
- All tests should be designed to pass (verify test logic is correct)
  </verify>
  <done>
Test page loads all required modules, runs 17 automated tests covering BlockDiagramView (SVG structure, components, active highlighting, instruction display), RegisterView (32 cells, highlighting), and CPUVisualizer integration (DOM structure, event handling, playback). Live demo shows interactive block diagram with step controls.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of SVG block diagram</name>
  <files>cpu-simulator/test-visualization.html</files>
  <action>
Human visually verifies the SVG block diagram visualization. This checkpoint confirms the block diagram looks like an actual hardware architecture diagram (matching the Duke ECE 350 reference style), components highlight correctly during animation, and the overall layout is professional and educational.

What was built: SVG block diagram visualization of the 5-stage pipelined processor, replacing the previous text-based stage cards. The diagram shows hardware components (Instruction Memory, Register File, ALU, Data Memory), pipeline registers (F/D, D/X, X/M, M/W as red bars), multiplexers (blue triangles), PC, adder, sign extend, and data path connections. Components highlight when their pipeline stage is active. Register grid is preserved below the diagram.
  </action>
  <verify>
1. Open `cpu-simulator/test-visualization.html` in a browser
2. Scroll to "Automated Tests" section - verify all tests pass (green checkmarks)
3. Scroll to "Live Demo" section
4. Verify the SVG block diagram is visible with labeled hardware components:
   - PC and +4 adder on far left
   - Instruction Memory
   - Red F/D bar
   - Register File and Sign Extend
   - Red D/X bar
   - ALU with blue mux triangle
   - Red X/M bar
   - Data Memory
   - Red M/W bar
   - Blue WB mux triangle
   - Data path wires connecting components
5. Click "Step Forward" several times:
   - Verify components highlight (green/blue/orange/purple fill) as their stage becomes active
   - Verify pipeline register text updates to show instruction (e.g., "ADDI $8, $0, 10")
   - Verify register grid below updates with yellow highlighting on changed registers
6. Hover over components - verify tooltips appear with component descriptions
7. Verify NO separate stage cards exist (the old "IF: ADD..." card layout is gone)
8. Verify NO separate cycle/instruction counter text exists
9. Resize browser window - verify diagram scales without distortion
  </verify>
  <done>
User confirms the SVG block diagram looks like a hardware architecture diagram, components highlight correctly during stepping, pipeline register instructions display properly, register grid works, and old stage cards/counters are gone. Type "approved" to continue or describe issues.
  </done>
</task>

</tasks>

<verification>
1. CPUVisualizer uses BlockDiagramView + RegisterView (no PipelineView, no ExecutionView)
2. Block diagram SVG renders all hardware components with proper grouping and data attributes
3. Active component highlighting works during animation playback
4. Pipeline register instruction displays update correctly
5. Register grid still functions with change highlighting
6. Test page passes all automated tests
7. Live demo shows interactive block diagram with step controls
8. No wire value labels on data paths (user decision honored)
9. Tooltips work via native SVG title elements (user decision honored)
10. Stage cards removed (user decision: block diagram replaces them)
11. Cycle/instruction counter removed (user decision)
</verification>

<success_criteria>
- CPUVisualizer coordinator wires BlockDiagramView and RegisterView correctly
- Test page validates SVG structure, component highlighting, and instruction display
- Live demo allows interactive stepping through a program with visual block diagram feedback
- User confirms the block diagram looks like a hardware architecture diagram
- No regressions in register grid functionality
</success_criteria>

<output>
After completion, create `.planning/phases/10-basic-visualization/10-04-SUMMARY.md`
</output>
