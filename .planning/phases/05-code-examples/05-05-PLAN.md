---
phase: 05-code-examples
plan: 05
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - docs/architecture.html

autonomous: true

must_haves:
  truths:
    - "Visitor can understand high-level engine architecture"
    - "Visitor can see how WASM compilation enables browser execution"
    - "Visitor can follow the pixel pipeline from draw call to screen"
    - "Page links together concepts from other doc pages"
  artifacts:
    - path: "docs/architecture.html"
      provides: "Architecture overview documentation page"
      min_lines: 250
      contains: "WASM"
  key_links:
    - from: "docs/architecture.html"
      to: "docs/rendering.html"
      via: "internal link"
      pattern: "href=\"rendering\\.html\""
    - from: "docs/architecture.html"
      to: "Emscripten"
      via: "content explanation"
      pattern: "Emscripten"
---

<objective>
Create the Architecture documentation page explaining how the graphics engine works, the WASM compilation process, and the pixel pipeline overview.

Purpose: Provides the "big picture" that ties other documentation together. Explains how C++ engine runs in browser via WebAssembly.
Output: docs/architecture.html - architecture overview with system diagrams and compilation explanation.
</objective>

<execution_context>
@/Users/orases/.claude/get-shit-done/workflows/execute-plan.md
@/Users/orases/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-code-examples/05-CONTEXT.md
@.planning/phases/05-code-examples/05-RESEARCH.md
@graphics-engine/wasm_bindings.cpp
@graphics-engine/Makefile.wasm
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare architecture content and diagrams</name>
  <files>Local analysis only - no file output</files>
  <action>
Plan the architecture page content:

**Section 1: Engine Overview**
- What is a 2D graphics engine?
- Core abstraction: Canvas that receives draw commands
- The rendering pipeline: commands -> rasterization -> pixels

**Section 2: C++ Implementation**
- Why C++ (performance, low-level control)
- Key classes: GCanvas, GPaint, GShader, GPath
- How they work together

**Section 3: WebAssembly Integration**
- What is WebAssembly (binary instruction format for browsers)
- Emscripten: C++ to WASM compiler
- EMSCRIPTEN_BINDINGS: Exposing C++ classes to JavaScript
- Read wasm_bindings.cpp to understand what's exposed

**Section 4: Pixel Pipeline**
- Draw call received (drawRect, drawConvexPolygon)
- Transform applied (CTM matrix)
- Edges computed and clipped
- Scanline rasterization
- Shader computes colors
- Blend mode composites
- Pixel written to buffer
- Buffer transferred to HTML5 Canvas

**Diagram ideas (describe in ASCII or simple HTML/CSS):**
1. Pipeline diagram: Draw Call -> Transform -> Rasterize -> Shade -> Blend -> Display
2. Class hierarchy: GCanvas contains GPaint contains GShader

**Code snippets to include:**
- From wasm_bindings.cpp: EMSCRIPTEN_BINDINGS block showing exposed classes
- From Makefile.wasm: Build command showing compilation flags
- Brief JS snippet showing how JavaScript calls WASM functions
  </action>
  <verify>
Confirm access to wasm_bindings.cpp and Makefile.wasm to show WASM integration.
  </verify>
  <done>
Architecture content planned with four main sections and diagram concepts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Architecture documentation page</name>
  <files>docs/architecture.html</files>
  <action>
Create docs/architecture.html with the following structure:

**HTML Head:**
- Title: "Architecture - Graphics Engine Documentation"
- Standard Prism.js CDN setup
- Local assets: docs.css, code.css, tabs.js

**Header:**
- Navigation to portfolio and other docs
- Title: "Engine Architecture"
- Subtitle: "How C++ graphics run in your browser"

**Introduction:**
- This page explains the "why" and "how" behind the demos
- Understanding architecture helps appreciate the implementation
- Bridge between traditional C++ development and modern web

**NO TABS for this page** - use sequential sections instead (architecture is linear narrative):

**Section 1: Engine Overview**
- What a 2D graphics engine does
- The abstraction: Canvas API receives draw commands, produces pixels
- This engine implements a subset of Skia/Cairo-like functionality
- Link to Rendering page for details on shapes

**Section 2: Class Architecture**
- GCanvas: The main drawing surface
- GPaint: Style information (color, blend mode, shader)
- GShader: Computes colors per-pixel (gradients, textures)
- GPath: Vector paths for complex shapes
- Simple ASCII or CSS-styled diagram showing relationships
- Link to Shaders page for shader details

**Section 3: The Pixel Pipeline**
- Step-by-step what happens when you call drawRect():
  1. Matrix transform applied to coordinates
  2. Rectangle converted to polygon edges
  3. Edges clipped to viewport
  4. For each scanline, edges intersected
  5. For each pixel, shader computes color
  6. Blend mode composites onto destination
  7. Final pixel written to bitmap
- Create visual pipeline diagram with HTML/CSS (boxes with arrows)
- Link to Advanced page for transforms and blend modes

**Section 4: WebAssembly Integration**
- Explain WASM benefits (near-native speed in browser)
- Emscripten compilation process
- Code snippet: Makefile.wasm showing em++ command with flags
- Code snippet: EMSCRIPTEN_BINDINGS from wasm_bindings.cpp
- How JavaScript communicates with WASM:
  - Module loading
  - Function calls
  - Memory sharing for pixel buffer
- Brief JavaScript snippet showing demo code calling engine

**Section 5: From C++ to Browser**
- The pixel buffer transfer (WASM memory -> ImageData -> Canvas)
- Real-time updates with requestAnimationFrame
- Why this approach vs pure JavaScript Canvas API

**Footer:**
- "Explore the implementation details in the other documentation pages"
- Links to: Rendering, Shaders, Advanced
- Link to main interactive demo

**CSS for diagrams:**
Create simple box diagrams using divs with borders, flexbox, and arrow characters or CSS pseudo-elements. No external diagram libraries needed.
  </action>
  <verify>
Open docs/architecture.html in browser:
1. Page loads without errors
2. Sequential sections flow logically
3. Pipeline diagram is visible and clear
4. Code snippets show WASM binding and compilation
5. Links to other doc pages work
6. Explanation is accessible to non-experts
  </verify>
  <done>
Architecture page provides complete overview of engine design and WASM integration.
  </done>
</task>

</tasks>

<verification>
All checks for this plan:
1. docs/architecture.html exists with 250+ lines
2. No tabs - uses sequential section layout
3. Pipeline diagram visible (HTML/CSS based)
4. WASM binding code shown from wasm_bindings.cpp
5. Build process explained with Makefile snippet
6. Links to other 3 doc pages present
7. Explanation connects C++ to browser execution
</verification>

<success_criteria>
- docs/architecture.html explains engine at system level
- WASM compilation and binding clearly documented
- Pixel pipeline walkthrough helps visitors understand rendering
- Class relationships explained
- Page serves as "glue" connecting other documentation pages
- Accessible to technical readers who aren't graphics experts
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-examples/05-05-SUMMARY.md`
</output>
