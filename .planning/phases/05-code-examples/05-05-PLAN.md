---
phase: 05-code-examples
plan: 05
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - docs/advanced-geometry.html

autonomous: true

must_haves:
  truths:
    - "Visitor can understand how Bezier curves are tessellated into line segments"
    - "Visitor can see the C++ algorithm for mesh triangle rendering"
    - "Visitor can follow how shader composition (ProxyShader, ComposeShader) works"
    - "Visitor can interact with demos showing meshes and curve-based shapes"
  artifacts:
    - path: "docs/advanced-geometry.html"
      provides: "Advanced Geometry documentation page (PA5 + PA6)"
      min_lines: 300
      contains: "drawMesh"
  key_links:
    - from: "docs/advanced-geometry.html"
      to: "wasm-graphics-demo.html#demo=mesh"
      via: "iframe src with hash"
      pattern: "#demo=mesh"
    - from: "docs/advanced-geometry.html"
      to: "wasm-graphics-demo.html#demo=paths"
      via: "iframe src with hash"
      pattern: "#demo=paths"
---

<objective>
Create the Advanced Geometry documentation page covering PA5 (curves, tessellation) and PA6 (triangles, drawMesh, drawQuad, shader composition) with C++ implementations and demos.

Purpose: Advanced geometry shows mastery of computer graphics. This page covers Bezier curve subdivision, triangle mesh rendering, and shader composition patterns.
Output: docs/advanced-geometry.html - comprehensive PA5+PA6 documentation with tabs for curves, meshes, and shader composition.
</objective>

<execution_context>
@/Users/orases/.claude/get-shit-done/workflows/execute-plan.md
@/Users/orases/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-code-examples/05-CONTEXT.md
@.planning/phases/05-code-examples/05-RESEARCH.md
@graphics-engine/path_ops.h
@graphics-engine/my_canvas.cpp
@graphics-engine/shader_ops.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Advanced Geometry documentation page</name>
  <files>docs/advanced-geometry.html</files>
  <action>
Create docs/advanced-geometry.html with the following structure:

**HTML Head:**
- Title: "Advanced Geometry (PA5 + PA6) - Graphics Engine Documentation"
- Same Prism.js CDN setup as other doc pages
- Local assets: docs.css, code.css, tabs.js

**Header:**
- Navigation back to docs index and portfolio
- Title: "Advanced Geometry"
- Subtitle: "PA5: Bezier Curves | PA6: Meshes and Shader Composition"

**Introduction:**
- Explain what PA5 and PA6 introduce
- PA5: Bezier curves (quadratic and cubic) with adaptive tessellation
- PA6: Triangle meshes (drawMesh), quad rendering (drawQuad), shader composition
- These features enable complex smooth shapes and texture mapping on arbitrary geometry

**Tabs structure with 4 tabs:**

**Tab 1: "Bezier Curves"**
- Explain Bezier curves: control points define smooth curves
- Quadratic (3 points) vs Cubic (4 points) Beziers
- de Casteljau algorithm for subdivision
- Adaptive tessellation: subdivide until "flat enough"
- Code snippet: Curve subdivision or flattening from path_ops.h
- Highlight the recursive subdivision or flatness test
- Extract 25-40 lines showing curve handling
- Demo: wasm-graphics-demo.html#demo=paths (curves are rendered via paths)
  - Instruction: "Paths containing curves are flattened to line segments. The tessellation algorithm decides how many segments are needed for smooth appearance."

**Tab 2: "Triangle Meshes"**
- Explain drawMesh: rendering textured triangle meshes
- Vertex attributes: position, texture coordinates (and optionally colors)
- Index buffer: how vertices are connected into triangles
- Why meshes matter: arbitrary geometry with texture mapping
- Code snippet: drawMesh() implementation from my_canvas.cpp
- Highlight the triangle iteration and vertex attribute interpolation
- Extract 30-45 lines showing mesh rendering
- Demo: wasm-graphics-demo.html#demo=mesh
  - Instruction: "The mesh demo shows triangles with interpolated texture coordinates. Drag vertices to see how the mesh deforms while maintaining texture mapping."

**Tab 3: "Quad Rendering"**
- Explain drawQuad: 4-point patch rendered as two triangles
- Bilinear interpolation for texture mapping across quad
- How quads simplify common use cases (rectangles, patches)
- Code snippet: drawQuad() from my_canvas.cpp (if it delegates to drawMesh or has its own logic)
- Highlight the quad to triangle conversion
- Extract 15-25 lines showing quad handling

**Tab 4: "Shader Composition"**
- Explain ProxyShader: wraps another shader with additional transformation
- Explain ComposeShader: combines two shaders (e.g., texture + gradient)
- Why composition matters: modular, reusable shader effects
- Code snippet: ProxyShader or ComposeShader from shader_ops.h
- Highlight how the composed shader calls multiple shaders
- Highlight the setContext chaining
- Extract 25-40 lines showing shader composition
- Note: Demo may be #demo=bitmap-shader or another that uses composed shaders

**Each tab structure:**
1. Algorithm explanation (2-3 paragraphs)
2. What the code accomplishes in plain English
3. Code snippet with line numbers and highlighted key lines
4. Instruction text for demo (where applicable)
5. Lazy-loaded iframe with correct #demo= hash

**Footer:**
- Links to other doc pages
- "Advanced geometry enables smooth curves, arbitrary mesh shapes, and flexible shader combinations"
  </action>
  <verify>
Open docs/advanced-geometry.html in browser:
1. Page loads without errors
2. All 4 tabs functional
3. Each tab has explanation + code
4. Demo iframes load correct preset via URL hash:
   - Bezier Curves tab: #demo=paths (or a curves-specific demo if available)
   - Triangle Meshes tab: #demo=mesh
5. Code highlighting and line numbers work
6. Can interact with mesh demo and see triangle rendering
  </verify>
  <done>
Advanced Geometry page complete with PA5+PA6 content: Bezier curves, triangle meshes, quad rendering, and shader composition documented with actual C++ code and demos.
  </done>
</task>

</tasks>

<verification>
All checks for this plan:
1. docs/advanced-geometry.html exists with 300+ lines
2. Four tabs: Bezier Curves, Triangle Meshes, Quad Rendering, Shader Composition
3. Curves tab has tessellation/subdivision from path_ops.h
4. Meshes tab shows drawMesh() from my_canvas.cpp
5. Quad tab shows drawQuad() handling
6. Shader Composition tab shows ProxyShader/ComposeShader
7. All tabs have syntax-highlighted C++ code
8. Demo iframes link to #demo=paths and #demo=mesh
9. Code-to-visual mapping: explanation -> code -> matching demo
</verification>

<success_criteria>
- docs/advanced-geometry.html provides comprehensive PA5+PA6 documentation
- Bezier curve tessellation algorithm explained
- Triangle mesh rendering with vertex attributes shown
- Quad to triangle conversion documented
- Shader composition (Proxy, Compose) patterns demonstrated
- C++ code snippets extracted from actual source files
- Demo iframes load correct presets via URL hash
- Visitor sees advanced graphics techniques with working code
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-examples/05-05-SUMMARY.md`
</output>
