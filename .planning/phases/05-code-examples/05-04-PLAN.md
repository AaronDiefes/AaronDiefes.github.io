---
phase: 05-code-examples
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - docs/paths-gradients.html

autonomous: true

must_haves:
  truths:
    - "Visitor can understand how GPath constructs complex shapes from commands"
    - "Visitor can see the C++ algorithm for winding number fill rule"
    - "Visitor can follow how linear and radial gradients interpolate colors"
    - "Visitor can interact with demos showing paths and gradient shaders"
  artifacts:
    - path: "docs/paths-gradients.html"
      provides: "Paths & Gradients documentation page (PA4)"
      min_lines: 300
      contains: "GPath"
  key_links:
    - from: "docs/paths-gradients.html"
      to: "wasm-graphics-demo.html#demo=paths"
      via: "iframe src with hash"
      pattern: "#demo=paths"
    - from: "docs/paths-gradients.html"
      to: "wasm-graphics-demo.html#demo=linear-gradient"
      via: "iframe src with hash"
      pattern: "#demo=linear-gradient"
    - from: "docs/paths-gradients.html"
      to: "wasm-graphics-demo.html#demo=radial-gradient"
      via: "iframe src with hash"
      pattern: "#demo=radial-gradient"
---

<objective>
Create the Paths & Gradients documentation page covering PA4 (GPath, winding fill, linear gradient, radial gradient) with C++ implementations and demos.

Purpose: Vector paths and gradient shaders are core computer graphics concepts. This page explains path construction, fill rules, and color interpolation algorithms.
Output: docs/paths-gradients.html - comprehensive PA4 documentation with tabs for paths and gradient types.
</objective>

<execution_context>
@/Users/orases/.claude/get-shit-done/workflows/execute-plan.md
@/Users/orases/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-code-examples/05-CONTEXT.md
@.planning/phases/05-code-examples/05-RESEARCH.md
@graphics-engine/path_ops.h
@graphics-engine/shader_ops.h
@graphics-engine/my_canvas.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Paths & Gradients documentation page</name>
  <files>docs/paths-gradients.html</files>
  <action>
Create docs/paths-gradients.html with the following structure:

**HTML Head:**
- Title: "Paths & Gradients (PA4) - Graphics Engine Documentation"
- Same Prism.js CDN setup as other doc pages
- Local assets: docs.css, code.css, tabs.js

**Header:**
- Navigation back to docs index and portfolio
- Title: "Paths & Gradients"
- Subtitle: "PA4: Vector Paths, Winding Fill, and Color Gradients"

**Introduction:**
- Explain what PA4 introduces: general paths and gradient shaders
- GPath allows building complex shapes from lines and curves
- Winding fill rule determines which pixels are "inside" the path
- Linear and radial gradients interpolate colors based on position

**Tabs structure with 4 tabs:**

**Tab 1: "Path Construction"**
- Explain GPath as a command-based path builder
- Commands: moveTo, lineTo, (and later cubicTo, quadTo)
- How paths are converted to edges for rasterization
- Code snippet: GPath command storage or iteration from path_ops.h
- Code snippet: Path to edges conversion (how moveTo/lineTo become Edge objects)
- Highlight the command iteration and edge building
- Extract 25-40 lines showing path construction or traversal

**Tab 2: "Winding Fill Rule"**
- Explain the winding number algorithm
- How crossing edges increment/decrement winding count
- Non-zero winding rule: pixel is inside if winding != 0
- Why this handles complex shapes (intersecting, nested)
- Code snippet: Winding count calculation from drawPath() in my_canvas.cpp
- Highlight the w += edge.winding logic
- Extract 20-35 lines showing fill rule implementation
- Demo: wasm-graphics-demo.html#demo=paths
  - Instruction: "Draw paths with the winding fill. Notice how overlapping regions are filled based on the winding number rule."

**Tab 3: "Linear Gradient"**
- Explain linear gradient math: projection onto gradient vector
- How position maps to t value (0 to 1)
- Color interpolation between gradient stops
- Code snippet: Linear gradient shadeRow() from shader_ops.h
- Highlight the dot product / projection calculation
- Highlight the t-value to color mapping
- Extract 30-45 lines of actual implementation
- Demo: wasm-graphics-demo.html#demo=linear-gradient
  - Instruction: "Adjust the gradient colors and see how the t-value interpolates between them. The projection onto the gradient line determines each pixel's color."

**Tab 4: "Radial Gradient"**
- Explain radial gradient: distance from center point
- How radius determines t value
- Circle of color radiating outward
- Code snippet: Radial gradient shadeRow() from shader_ops.h
- Highlight the sqrt(dx*dx + dy*dy) distance calculation
- Highlight the t normalization by radius
- Extract 25-40 lines of implementation
- Demo: wasm-graphics-demo.html#demo=radial-gradient
  - Instruction: "Watch how the gradient radiates from the center point. The distance calculation determines each pixel's color."

**Each tab structure:**
1. Algorithm explanation (2-3 paragraphs with mathematical intuition)
2. What the formula computes in plain English
3. Code snippet with line numbers and highlighted key lines
4. Instruction text for demo
5. Lazy-loaded iframe with correct #demo= hash

**Footer:**
- Links to other doc pages
- "Paths enable complex shapes; gradients enable smooth color transitions"
  </action>
  <verify>
Open docs/paths-gradients.html in browser:
1. Page loads without errors
2. All 4 tabs functional
3. Each tab has explanation + code + demo iframe (except Path Construction which shows code only)
4. Demo iframes load correct preset via URL hash:
   - Winding Fill tab: #demo=paths
   - Linear Gradient tab: #demo=linear-gradient
   - Radial Gradient tab: #demo=radial-gradient
5. Code highlighting and line numbers work
6. Can interact with demos and see gradient/path rendering
  </verify>
  <done>
Paths & Gradients page complete with PA4 content: GPath construction, winding fill rule, linear gradient, and radial gradient documented with actual C++ code and demos.
  </done>
</task>

</tasks>

<verification>
All checks for this plan:
1. docs/paths-gradients.html exists with 300+ lines
2. Four tabs: Path Construction, Winding Fill Rule, Linear Gradient, Radial Gradient
3. Path tab has GPath structure/traversal from path_ops.h
4. Winding tab shows fill rule from my_canvas.cpp
5. Linear Gradient tab shows shadeRow() implementation
6. Radial Gradient tab shows distance-based shading
7. All tabs have syntax-highlighted C++ code
8. Demo iframes link to #demo=paths, #demo=linear-gradient, #demo=radial-gradient
9. Code-to-visual mapping: explanation -> code -> matching demo
</verification>

<success_criteria>
- docs/paths-gradients.html provides comprehensive PA4 documentation
- GPath construction and command iteration explained
- Winding fill rule algorithm clearly shown with code
- Linear gradient projection math demonstrated
- Radial gradient distance calculation demonstrated
- C++ code snippets extracted from actual shader_ops.h and path_ops.h
- Demo iframes load correct presets via URL hash
- Visitor sees direct correspondence between code and demo
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-examples/05-04-SUMMARY.md`
</output>
