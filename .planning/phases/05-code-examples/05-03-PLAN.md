---
phase: 05-code-examples
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - docs/shaders.html

autonomous: true

must_haves:
  truths:
    - "Visitor can read explanation of shader architecture"
    - "Visitor can see C++ code for each gradient type implementation"
    - "Visitor can switch between Linear, Radial, Sweep, and Bitmap shader tabs"
    - "Visitor can interact with embedded gradient demos"
  artifacts:
    - path: "docs/shaders.html"
      provides: "Shaders system documentation page"
      min_lines: 300
      contains: "LinearGradient"
  key_links:
    - from: "docs/shaders.html"
      to: "docs/assets/js/tabs.js"
      via: "script src"
      pattern: "assets/js/tabs\\.js"
    - from: "docs/shaders.html"
      to: "wasm-graphics-demo.html#demo=linear-gradient"
      via: "iframe src with hash"
      pattern: "#demo="
---

<objective>
Create the Shaders System documentation page covering all gradient types (linear, radial, sweep) and bitmap shaders with C++ implementations and demos.

Purpose: Shaders are the most technically impressive part of the engine. This page showcases gradient interpolation algorithms and shader architecture.
Output: docs/shaders.html - comprehensive shader documentation with tabs for each shader type.
</objective>

<execution_context>
@/Users/orases/.claude/get-shit-done/workflows/execute-plan.md
@/Users/orases/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-code-examples/05-CONTEXT.md
@.planning/phases/05-code-examples/05-RESEARCH.md
@graphics-engine/shader_ops.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shader code snippets from shader_ops.h</name>
  <files>Local analysis only - no file output</files>
  <action>
Read graphics-engine/shader_ops.h and identify key code sections:

**Bitmap Shader (MyBMShader class):**
1. Constructor and matrix setup (~10 lines)
2. shadeRow() method showing tile modes (repeat, mirror, clamp) (~30-40 lines)
3. Tile mode helper functions (mirrorX, repeatX, clamp_x) (~15 lines each)

**Linear Gradient Shader:**
4. Find the linear gradient class
5. Identify the shadeRow() implementation showing t calculation along gradient vector
6. Color interpolation logic

**Radial Gradient Shader:**
7. Find radial gradient class
8. shadeRow() with distance-from-center calculation
9. Circle equation usage

**Sweep/Angle Gradient Shader:**
10. Find angle/sweep gradient class
11. shadeRow() with atan2 angle calculation
12. Angle-to-color mapping

For each shader type:
- Extract the core algorithm (20-40 lines)
- Identify lines to highlight (the mathematical insight)
- Note which demo to link (#demo=linear-gradient, etc.)
- Write 2-3 paragraph explanation of the algorithm

If some shader classes are in different files or structured differently, adapt to actual code structure.
  </action>
  <verify>
Confirm shader_ops.h contains implementations for:
- Bitmap shader with tile modes
- Linear gradient (or identify where it is)
- Radial gradient
- Sweep/angle gradient
  </verify>
  <done>
All four shader implementations located with code excerpts and demo mappings identified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Shaders System documentation page</name>
  <files>docs/shaders.html</files>
  <action>
Create docs/shaders.html with the following structure:

**HTML Head:**
- Title: "Shaders System - Graphics Engine Documentation"
- Same Prism.js CDN setup as rendering.html
- Local assets: docs.css, code.css, tabs.js

**Header:**
- Navigation back to portfolio and other docs
- Title: "Shaders System"
- Subtitle: "Gradients, textures, and color computation"

**Introduction:**
- Explain shader concept (objects that compute colors for each pixel)
- GShader interface overview (setContext, shadeRow)
- Why shaders enable complex effects without hardcoding

**Tabs structure with 4 tabs:**

**Tab 1: "Linear Gradient"**
- Explain linear gradient math (projection onto gradient vector)
- How t value maps position to color
- Code snippet: Linear gradient shadeRow() with t calculation
- Highlight the dot product / projection line
- Demo iframe: wasm-graphics-demo.html#demo=linear-gradient

**Tab 2: "Radial Gradient"**
- Explain radial gradient (distance from center)
- How radius determines t value
- Code snippet: Radial gradient shadeRow() with distance calculation
- Highlight sqrt(dx*dx + dy*dy) or equivalent
- Demo iframe: wasm-graphics-demo.html#demo=radial-gradient

**Tab 3: "Sweep Gradient"**
- Explain sweep/angle gradient (angle around center point)
- How atan2 converts position to angle
- Code snippet: Sweep gradient shadeRow() with atan2
- Highlight the angle calculation and normalization
- Demo iframe: wasm-graphics-demo.html#demo=sweep-gradient

**Tab 4: "Bitmap Shader"**
- Explain texture mapping concept
- Tile modes: clamp (extend edge), repeat (wrap), mirror (ping-pong)
- Code snippet: Bitmap shader shadeRow() with tile mode handling
- Highlight the tile mode switch/conditionals
- Demo iframe: wasm-graphics-demo.html#demo=bitmap-shader

**Each tab should have:**
1. Algorithm explanation (2-3 paragraphs)
2. Mathematical intuition (what the formula computes)
3. Code snippet with line numbers and highlights
4. Interactive demo at the end

**Footer:**
- Links to other doc pages
- "The shaders demonstrated here power all the gradient effects in the interactive demo"
  </action>
  <verify>
Open docs/shaders.html in browser:
1. Page loads without errors
2. All 4 shader tabs functional
3. Each tab has explanation + code + demo
4. Demo iframes load correct preset via URL hash
5. Code highlighting and line numbers work
  </verify>
  <done>
Shaders System page complete with all four shader types documented, code shown, and demos embedded.
  </done>
</task>

</tasks>

<verification>
All checks for this plan:
1. docs/shaders.html exists with 300+ lines
2. Four tabs: Linear Gradient, Radial Gradient, Sweep Gradient, Bitmap Shader
3. Each tab has algorithm explanation
4. Each tab has syntax-highlighted C++ code
5. Each tab has working demo iframe with correct hash
6. Demo presets match tab content (linear shows linear gradient, etc.)
7. Navigation links to other doc pages
</verification>

<success_criteria>
- docs/shaders.html provides comprehensive shader documentation
- All four shader types explained with algorithm details
- C++ code snippets accurately represent shader_ops.h implementation
- Demo iframes load correct presets showing each shader type
- Technical explanations are accurate and accessible
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-examples/05-03-SUMMARY.md`
</output>
