---
phase: 05-code-examples
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - docs/transforms-textures.html

autonomous: true

must_haves:
  truths:
    - "Visitor can understand how 2D affine matrices transform coordinates"
    - "Visitor can see the C++ implementation of CTM stack (save/restore)"
    - "Visitor can follow how bitmap shaders sample and tile textures"
    - "Visitor can interact with demos showing transforms and bitmap textures"
  artifacts:
    - path: "docs/transforms-textures.html"
      provides: "Transformations & Textures documentation page (PA3)"
      min_lines: 250
      contains: "GMatrix"
  key_links:
    - from: "docs/transforms-textures.html"
      to: "docs/assets/js/tabs.js"
      via: "script src"
      pattern: "assets/js/tabs\\.js"
    - from: "docs/transforms-textures.html"
      to: "wasm-graphics-demo.html#demo=transforms"
      via: "iframe src with hash"
      pattern: "#demo=transforms"
    - from: "docs/transforms-textures.html"
      to: "wasm-graphics-demo.html#demo=bitmap-shader"
      via: "iframe src with hash"
      pattern: "#demo=bitmap"
---

<objective>
Create the Transformations & Textures documentation page covering PA3 (matrices, CTM stack, bitmap shader, tile modes) with C++ implementations and demos.

Purpose: Matrix transformations and bitmap shaders are fundamental to computer graphics. This page shows how coordinates are transformed and how textures are sampled.
Output: docs/transforms-textures.html - comprehensive PA3 documentation with tabs for matrices and bitmap shaders.
</objective>

<execution_context>
@/Users/orases/.claude/get-shit-done/workflows/execute-plan.md
@/Users/orases/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-code-examples/05-CONTEXT.md
@.planning/phases/05-code-examples/05-RESEARCH.md
@graphics-engine/matrix_transform.cpp
@graphics-engine/shader_ops.h
@graphics-engine/my_canvas.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Transformations & Textures documentation page</name>
  <files>docs/transforms-textures.html</files>
  <action>
Create docs/transforms-textures.html with the following structure:

**HTML Head:**
- Title: "Transformations & Textures (PA3) - Graphics Engine Documentation"
- Same Prism.js CDN setup as other doc pages
- Local assets: docs.css, code.css, tabs.js

**Header:**
- Navigation back to docs index and portfolio
- Title: "Transformations & Textures"
- Subtitle: "PA3: Matrices, CTM Stack, and Bitmap Shaders"

**Introduction:**
- Explain what PA3 introduces: matrix transformations and texture mapping
- GMatrix represents 2D affine transformations (3x3 matrix with homogeneous coords)
- CTM (Current Transform Matrix) allows composing multiple transformations
- Bitmap shaders enable texture mapping with different tile modes

**Tabs structure with 3 tabs:**

**Tab 1: "Matrix Fundamentals"**
- Explain 2D affine transformation matrices
- The 3x3 matrix structure: [a b c; d e f; 0 0 1]
- What translate, rotate, scale do mathematically
- Matrix composition: multiplying matrices combines transformations
- Code snippet: GMatrix structure or key methods from matrix_transform.cpp or include file
- Code snippet: translate(), rotate(), scale() static constructors if available
- Highlight the matrix multiplication or point mapping
- Extract 25-40 lines showing matrix math
- Demo: wasm-graphics-demo.html#demo=transforms
  - Instruction: "Adjust rotation, scale, and translation to see how the CTM transforms the rendered shape."

**Tab 2: "CTM Stack"**
- Explain the transformation stack concept
- save() pushes current matrix, restore() pops back
- concat() multiplies new matrix into CTM
- Why this matters: nested transformations, scene graphs
- Code snippet: save()/restore() showing stack push/pop from my_canvas.cpp
- Code snippet: concat() showing matrix multiplication
- Highlight the fMatrixStack usage
- Extract 20-30 lines showing CTM operations

**Tab 3: "Bitmap Shader"**
- Explain texture mapping concept
- How the shader transforms canvas coordinates to texture coordinates
- Tile modes: Clamp (extend edge color), Repeat (wrap around), Mirror (ping-pong)
- Code snippet: MyBMShader or bitmap shader shadeRow() from shader_ops.h
- Highlight the tile mode handling (switch/if for clamp, repeat, mirror)
- Highlight the texel sampling: how x,y map to bitmap pixel
- Extract 30-50 lines showing bitmap sampling and tile modes
- Demo: wasm-graphics-demo.html#demo=bitmap-shader
  - Instruction: "Change the tile mode to see how the texture extends beyond its bounds. Watch how Clamp extends edge pixels, Repeat wraps, and Mirror ping-pongs."

**Each tab structure:**
1. Algorithm explanation (2-3 paragraphs with mathematical intuition)
2. What the formula/code computes in plain English
3. Code snippet with line numbers and highlighted key lines
4. Instruction text explaining what to look for in demo
5. Lazy-loaded iframe: src="../wasm-graphics-demo.html#demo=XXX" loading="lazy" title="[Topic] demo"

**Footer:**
- Links to other doc pages (placeholder text for pages not yet built)
- "Matrix transformations enable complex positioning; bitmap shaders enable texture mapping"
  </action>
  <verify>
Open docs/transforms-textures.html in browser:
1. Page loads without errors
2. All 3 tabs functional
3. Each tab has explanation + code + demo iframe (where applicable)
4. Demo iframes load correct preset via URL hash:
   - Matrix Fundamentals tab: #demo=transforms
   - Bitmap Shader tab: #demo=bitmap-shader
5. Code highlighting and line numbers work
6. Can interact with demo controls and see transform/texture changes
  </verify>
  <done>
Transformations & Textures page complete with PA3 content: matrix math, CTM stack operations, and bitmap shader with tile modes documented with actual C++ code and demos.
  </done>
</task>

</tasks>

<verification>
All checks for this plan:
1. docs/transforms-textures.html exists with 250+ lines
2. Three tabs: Matrix Fundamentals, CTM Stack, Bitmap Shader
3. Matrix tab has matrix math from matrix_transform.cpp
4. CTM Stack tab shows save/restore/concat from my_canvas.cpp
5. Bitmap Shader tab shows shading with tile modes from shader_ops.h
6. All tabs have syntax-highlighted C++ code
7. Demo iframes link to #demo=transforms and #demo=bitmap-shader
8. Code-to-visual mapping: explanation -> code -> matching demo
</verification>

<success_criteria>
- docs/transforms-textures.html provides comprehensive PA3 documentation
- Matrix math explained with code showing actual GMatrix operations
- CTM stack operations (save/restore/concat) clearly shown
- Bitmap shader tile modes (clamp, repeat, mirror) explained and demonstrated
- C++ code snippets extracted from actual engine source files
- Demo iframes load correct presets via URL hash
- Visitor sees direct correspondence between code and demo
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-examples/05-03-SUMMARY.md`
</output>
