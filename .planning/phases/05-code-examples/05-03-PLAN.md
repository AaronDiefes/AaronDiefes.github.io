---
phase: 05-code-examples
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - docs/shaders.html

autonomous: true

must_haves:
  truths:
    - "Visitor can understand how shaders compute per-pixel colors"
    - "Visitor can see the C++ implementation for each gradient type"
    - "Visitor can interact with demos that directly show each shader working"
  artifacts:
    - path: "docs/shaders.html"
      provides: "Shaders system documentation page"
      min_lines: 300
      contains: "LinearGradient"
  key_links:
    - from: "docs/shaders.html"
      to: "docs/assets/js/tabs.js"
      via: "script src"
      pattern: "assets/js/tabs\\.js"
    - from: "docs/shaders.html"
      to: "wasm-graphics-demo.html#demo=linear-gradient"
      via: "iframe src with hash"
      pattern: "#demo=linear-gradient"
    - from: "docs/shaders.html"
      to: "wasm-graphics-demo.html#demo=radial-gradient"
      via: "iframe src with hash"
      pattern: "#demo=radial-gradient"
---

<objective>
Create the Shaders System documentation page covering all gradient types (linear, radial, sweep) and bitmap shaders with C++ implementations and demos.

Purpose: Shaders are the most technically impressive part of the engine. This page showcases gradient interpolation algorithms and shader architecture.
Output: docs/shaders.html - comprehensive shader documentation with tabs for each shader type.
</objective>

<execution_context>
@/Users/orases/.claude/get-shit-done/workflows/execute-plan.md
@/Users/orases/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-code-examples/05-CONTEXT.md
@.planning/phases/05-code-examples/05-RESEARCH.md
@graphics-engine/shader_ops.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Shaders System documentation page</name>
  <files>docs/shaders.html</files>
  <action>
Create docs/shaders.html with the following structure:

**HTML Head:**
- Title: "Shaders System - Graphics Engine Documentation"
- Same Prism.js CDN setup as rendering.html
- Local assets: docs.css, code.css, tabs.js

**Header:**
- Navigation back to portfolio and docs index
- Title: "Shaders System"
- Subtitle: "Gradients, textures, and color computation"

**Introduction:**
- Explain shader concept (objects that compute colors for each pixel)
- GShader interface overview (setContext, shadeRow)
- Why shaders enable complex effects without hardcoding

**Tabs structure with 4 tabs:**

**Tab 1: "Linear Gradient"**
- Explain linear gradient math (projection onto gradient vector)
- How t value maps position to color
- Code snippet: Linear gradient shadeRow() with t calculation from shader_ops.h
- Highlight the dot product / projection line
- Extract 20-40 lines of actual implementation
- Demo: wasm-graphics-demo.html#demo=linear-gradient
  - Instruction: "The demo below shows linear gradients. Adjust colors and positions to see the t-value interpolation in action."

**Tab 2: "Radial Gradient"**
- Explain radial gradient (distance from center)
- How radius determines t value
- Code snippet: Radial gradient shadeRow() with distance calculation from shader_ops.h
- Highlight sqrt(dx*dx + dy*dy) or equivalent
- Demo: wasm-graphics-demo.html#demo=radial-gradient
  - Instruction: "Watch how the gradient radiates from the center point. The distance calculation determines each pixel's color."

**Tab 3: "Sweep Gradient"**
- Explain sweep/angle gradient (angle around center point)
- How atan2 converts position to angle
- Code snippet: Sweep gradient shadeRow() with atan2 from shader_ops.h
- Highlight the angle calculation and normalization
- Demo: wasm-graphics-demo.html#demo=sweep-gradient
  - Instruction: "The sweep gradient uses trigonometry to map angles to colors."

**Tab 4: "Bitmap Shader"**
- Explain texture mapping concept
- Tile modes: clamp (extend edge), repeat (wrap), mirror (ping-pong)
- Code snippet: Bitmap shader shadeRow() with tile mode handling from shader_ops.h
- Highlight the tile mode switch/conditionals
- Demo: wasm-graphics-demo.html#demo=bitmap-shader
  - Instruction: "Change the tile mode to see how the texture extends beyond its bounds."

**Each tab structure:**
1. Algorithm explanation (2-3 paragraphs with mathematical intuition)
2. What the formula computes in plain English
3. Code snippet with line numbers and highlighted key lines
4. Instruction text explaining what to look for
5. Lazy-loaded iframe: src="../wasm-graphics-demo.html#demo=XXX" loading="lazy" title="[Shader type] demo"

**Footer:**
- Placeholder links to other doc pages (same pattern as rendering.html)
- "The shaders demonstrated here power all the gradient effects in the interactive demo"
  </action>
  <verify>
Open docs/shaders.html in browser:
1. Page loads without errors
2. All 4 shader tabs functional
3. Each tab has explanation + code + demo iframe
4. Demo iframes load correct preset via URL hash:
   - Linear Gradient tab: #demo=linear-gradient
   - Radial Gradient tab: #demo=radial-gradient
   - Sweep Gradient tab: #demo=sweep-gradient
   - Bitmap Shader tab: #demo=bitmap-shader
5. Code highlighting and line numbers work
6. Can interact with demo controls and see shader changes
  </verify>
  <done>
Shaders System page complete with all four shader types documented, actual C++ code shown, and demos embedded with correct URL hashes that directly correspond to the shader being explained.
  </done>
</task>

</tasks>

<verification>
All checks for this plan:
1. docs/shaders.html exists with 300+ lines
2. Four tabs: Linear Gradient, Radial Gradient, Sweep Gradient, Bitmap Shader
3. Each tab has algorithm explanation
4. Each tab has syntax-highlighted C++ code from shader_ops.h
5. Each tab has iframe with correct #demo= hash
6. Demo presets match tab content (linear shows linear gradient, etc.)
7. Code-to-visual mapping: explanation -> code -> matching demo
</verification>

<success_criteria>
- docs/shaders.html provides comprehensive shader documentation
- All four shader types explained with algorithm details
- C++ code snippets extracted from actual shader_ops.h
- Demo iframes load correct presets via URL hash
- Visitor sees direct correspondence: read about linear gradient -> see linear gradient code -> interact with linear gradient demo
- Technical explanations are accurate and accessible
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-examples/05-03-SUMMARY.md`
</output>
