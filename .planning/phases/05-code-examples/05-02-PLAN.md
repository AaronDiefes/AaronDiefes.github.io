---
phase: 05-code-examples
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - docs/rendering.html

autonomous: true

must_haves:
  truths:
    - "Visitor can understand how the graphics engine creates and manages canvases"
    - "Visitor can see the C++ algorithms for rendering shapes"
    - "Visitor can interact with a live demo showing the rendering concepts"
  artifacts:
    - path: "docs/rendering.html"
      provides: "Rendering basics documentation page"
      min_lines: 200
      contains: "language-cpp"
  key_links:
    - from: "docs/rendering.html"
      to: "docs/assets/css/docs.css"
      via: "link rel stylesheet"
      pattern: "assets/css/docs\\.css"
    - from: "docs/rendering.html"
      to: "wasm-graphics-demo.html"
      via: "iframe src with hash parameter"
      pattern: "wasm-graphics-demo\\.html#demo="
---

<objective>
Create the Rendering Basics documentation page explaining canvas fundamentals, shape drawing, and color handling with C++ code snippets and embedded demo.

Purpose: First documentation page showing technical depth of the graphics engine. Covers foundational concepts that other pages build upon.
Output: docs/rendering.html - complete documentation page with interleaved explanation, code, and demo.
</objective>

<execution_context>
@/Users/orases/.claude/get-shit-done/workflows/execute-plan.md
@/Users/orases/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-code-examples/05-CONTEXT.md
@.planning/phases/05-code-examples/05-RESEARCH.md
@graphics-engine/my_canvas.cpp
@graphics-engine/my_utils.h
@graphics-engine/Edge.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Rendering Basics documentation page</name>
  <files>docs/rendering.html</files>
  <action>
Create docs/rendering.html with the following structure:

**HTML Head:**
- Title: "Rendering Basics - Graphics Engine Documentation"
- Meta viewport for responsive
- Prism.js CDN resources (same as test-assets.html)
- Local CSS: docs.css, code.css
- Local JS: tabs.js (defer)

**Header:**
- Navigation back to main portfolio (index.html)
- Title: "Rendering Basics"
- Subtitle: "Canvas, shapes, and color fundamentals"

**Introduction section:**
- Brief overview of the rendering pipeline (2-3 paragraphs)
- What this page covers: canvas creation, pixel manipulation, shape rendering

**Tabs structure with 3-4 tabs:**

**Tab 1: "Canvas Creation"**
- Explain GBitmap and pixel buffer concept
- Code snippet: GCreateCanvas factory + clear() implementation from my_canvas.cpp
- Highlight the blitRow loop
- Extract 20-40 lines showing the core algorithm

**Tab 2: "Color Handling"**
- Explain premultiplied alpha and why it matters
- Code snippet: unpremult() or color conversion from my_utils.h
- Highlight the channel multiplication
- Extract key color math lines

**Tab 3: "Shape Drawing"**
- Explain edge-based rendering approach
- Code snippet: drawConvexPolygon() with edge building from my_canvas.cpp
- Highlight the edge sorting and clipping
- Extract the polygon rendering core

**Tab 4: "Edge Clipping"**
- Explain why clipping is needed (viewport bounds)
- Code snippet: Edge::clip() or edge construction from Edge.h
- Highlight the boundary checks

**Demo section:**
- Heading: "Try It: Interactive Shapes"
- Brief instruction text: "This demo runs the C++ engine compiled to WebAssembly. Try drawing shapes to see the rendering pipeline in action."
- iframe: src="../wasm-graphics-demo.html#demo=shapes" loading="lazy" title="Interactive shapes demo" width="100%" height="600"
- The #demo=shapes hash parameter loads the shapes preset, directly corresponding to the rendering code shown above

**Footer:**
- Text: "Continue exploring:" followed by placeholder links
- Links will be: Shaders System, Advanced Features, Architecture (these pages don't exist yet during wave 2 execution, so use placeholder text like "[Shaders System coming soon]" or just list the topics)
- Link back to main demo

**Code block requirements:**
- Use class="line-numbers" on pre elements
- Use data-line="X-Y" for highlighted lines (identify key algorithm lines)
- Use class="language-cpp" on code elements
- HTML-escape all angle brackets in C++ code (&lt; &gt;)
- Read actual source files to extract real code snippets
  </action>
  <verify>
Open docs/rendering.html in browser:
1. Page loads without errors (check console)
2. All 4 tabs functional (click switches content)
3. Code has syntax highlighting (colored keywords)
4. Line numbers display on left side
5. Highlighted lines have yellow/gold background
6. Copy button appears in code toolbar
7. Demo iframe loads wasm-graphics-demo.html#demo=shapes
8. Iframe shows shapes demo (rectangle/polygon drawing)
  </verify>
  <done>
Rendering Basics page is complete with interleaved explanation, syntax-highlighted C++ code from actual source files, and working embedded demo that matches the code being explained.
  </done>
</task>

</tasks>

<verification>
All checks for this plan:
1. docs/rendering.html exists with 200+ lines
2. Page includes all CDN and local asset references
3. Four tabs with distinct content (Canvas, Color, Shape, Edge)
4. Each tab has explanatory text + C++ code snippet
5. Code blocks have line-numbers class
6. At least one code block has data-line highlighting
7. Iframe src includes #demo=shapes hash parameter
8. Demo iframe loads and shows shapes preset
</verification>

<success_criteria>
- docs/rendering.html displays complete documentation for rendering fundamentals
- C++ code snippets are extracted from actual my_canvas.cpp, my_utils.h, Edge.h files
- Syntax highlighting, line numbers, and copy work correctly
- Demo iframe loads shapes preset via URL hash
- Code-to-demo mapping: visitor reads about shape rendering, then sees shape rendering in demo
- Page is readable and explains concepts clearly to technical audience
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-examples/05-02-SUMMARY.md`
</output>
