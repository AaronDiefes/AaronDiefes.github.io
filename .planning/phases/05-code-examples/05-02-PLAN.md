---
phase: 05-code-examples
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - docs/rendering.html

autonomous: true

must_haves:
  truths:
    - "Visitor can read explanation of canvas rendering fundamentals"
    - "Visitor can see C++ code for shape drawing operations"
    - "Visitor can interact with embedded demo showing shapes"
    - "Code snippets have syntax highlighting, line numbers, copy button"
  artifacts:
    - path: "docs/rendering.html"
      provides: "Rendering basics documentation page"
      min_lines: 200
      contains: "language-cpp"
  key_links:
    - from: "docs/rendering.html"
      to: "docs/assets/css/docs.css"
      via: "link rel stylesheet"
      pattern: "assets/css/docs\\.css"
    - from: "docs/rendering.html"
      to: "wasm-graphics-demo.html"
      via: "iframe src"
      pattern: "iframe.*wasm-graphics-demo"
---

<objective>
Create the Rendering Basics documentation page explaining canvas fundamentals, shape drawing, and color handling with C++ code snippets and embedded demo.

Purpose: First documentation page showing technical depth of the graphics engine. Covers foundational concepts that other pages build upon.
Output: docs/rendering.html - complete documentation page with interleaved explanation, code, and demo.
</objective>

<execution_context>
@/Users/orases/.claude/get-shit-done/workflows/execute-plan.md
@/Users/orases/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-code-examples/05-CONTEXT.md
@.planning/phases/05-code-examples/05-RESEARCH.md
@graphics-engine/my_canvas.cpp
@graphics-engine/my_utils.h
@graphics-engine/Edge.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract and prepare code snippets from C++ source</name>
  <files>Local analysis only - no file output</files>
  <action>
Read and identify the key code sections to extract from the C++ engine:

**From my_canvas.cpp:**
1. `clear()` function - Basic canvas clearing with color
2. `drawRect()` function - Rectangle drawing (delegates to polygon)
3. `drawConvexPolygon()` function - Core polygon rendering with edge building
4. `GCreateCanvas()` factory function - Canvas instantiation

**From my_utils.h:**
5. `unpremult()` or color conversion functions
6. `div255()` fast division helper

**From Edge.h:**
7. Edge structure and `clip()` function - Edge construction and clipping

For each snippet:
- Identify line range (target 20-40 lines showing algorithm core)
- Note which lines to highlight (the key algorithmic insight)
- Prepare HTML-escaped version (escape &lt; &gt; &amp;)
- Write brief explanation (2-3 sentences) for what the code demonstrates

This is analysis work to prepare content for Task 2.
  </action>
  <verify>
Confirm access to all source files and identify 4-5 code snippets covering:
- Canvas creation
- Color handling
- Shape rendering
- Edge construction
  </verify>
  <done>
Code snippets identified with line highlights and explanations ready for page assembly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Rendering Basics documentation page</name>
  <files>docs/rendering.html</files>
  <action>
Create docs/rendering.html with the following structure:

**HTML Head:**
- Title: "Rendering Basics - Graphics Engine Documentation"
- Meta viewport for responsive
- Prism.js CDN resources (same as test-assets.html)
- Local CSS: docs.css, code.css
- Local JS: tabs.js (defer)

**Header:**
- Navigation back to main portfolio (index.html)
- Title: "Rendering Basics"
- Subtitle: "Canvas, shapes, and color fundamentals"

**Introduction section:**
- Brief overview of the rendering pipeline (2-3 paragraphs)
- What this page covers: canvas creation, pixel manipulation, shape rendering

**Tabs structure with 3-4 tabs:**

**Tab 1: "Canvas Creation"**
- Explain GBitmap and pixel buffer concept
- Code snippet: GCreateCanvas factory + clear() implementation
- Highlight the blitRow loop

**Tab 2: "Color Handling"**
- Explain premultiplied alpha and why it matters
- Code snippet: unpremult() or color conversion
- Highlight the channel multiplication

**Tab 3: "Shape Drawing"**
- Explain edge-based rendering approach
- Code snippet: drawConvexPolygon() with edge building
- Highlight the edge sorting and clipping

**Tab 4: "Edge Clipping"**
- Explain why clipping is needed (viewport bounds)
- Code snippet: Edge::clip() or edge construction
- Highlight the boundary checks

**Demo section:**
- Heading: "Try It: Interactive Shapes"
- Brief instruction text
- Lazy-loaded iframe to wasm-graphics-demo.html (no hash needed - shapes is default)
- Iframe: loading="lazy", title attribute, width 100%, height 600

**Footer:**
- Links to other doc pages (Shaders, Advanced, Architecture)
- Link back to main demo

**Code block requirements:**
- Use class="line-numbers" on pre elements
- Use data-line="X-Y" for highlighted lines
- Use class="language-cpp" on code elements
- HTML-escape all angle brackets in C++ code
  </action>
  <verify>
Open docs/rendering.html in browser:
1. Page loads without errors
2. All 4 tabs functional
3. Code has syntax highlighting
4. Line numbers display
5. Highlighted lines visible
6. Copy button works
7. Demo iframe loads (may be slow first time due to WASM)
  </verify>
  <done>
Rendering Basics page is complete with interleaved explanation, syntax-highlighted C++ code, and working embedded demo.
  </done>
</task>

</tasks>

<verification>
All checks for this plan:
1. docs/rendering.html exists with 200+ lines
2. Page includes all CDN and local asset references
3. Four tabs with distinct content
4. Each tab has explanatory text + code snippet
5. Code blocks have line-numbers class
6. At least one code block has data-line highlighting
7. Iframe loads wasm-graphics-demo.html
8. Page navigation links present
</verification>

<success_criteria>
- docs/rendering.html displays complete documentation for rendering fundamentals
- C++ code snippets accurately represent my_canvas.cpp implementation
- Syntax highlighting, line numbers, and copy work
- Demo iframe loads and is interactive
- Page is readable and explains concepts clearly
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-examples/05-02-SUMMARY.md`
</output>
