---
phase: 05-code-examples
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - docs/core-rendering.html

autonomous: true

must_haves:
  truths:
    - "Visitor can understand how shapes are rasterized using edge-based rendering"
    - "Visitor can see the C++ algorithm for Porter-Duff alpha blending"
    - "Visitor can follow how convex polygons are clipped to the viewport"
    - "Visitor can interact with a live demo showing shapes and blend modes"
  artifacts:
    - path: "docs/core-rendering.html"
      provides: "Core Rendering documentation page (PA1 + PA2)"
      min_lines: 250
      contains: "language-cpp"
  key_links:
    - from: "docs/core-rendering.html"
      to: "docs/assets/css/docs.css"
      via: "link rel stylesheet"
      pattern: "assets/css/docs\\.css"
    - from: "docs/core-rendering.html"
      to: "wasm-graphics-demo.html"
      via: "iframe src with hash parameter"
      pattern: "wasm-graphics-demo\\.html#demo="
---

<objective>
Create the Core Rendering documentation page covering PA1 (lines, rectangles, blending) and PA2 (convex polygons, clipping) with C++ code snippets and embedded demos.

Purpose: First documentation page showing foundational rendering algorithms. Covers shape rasterization, edge-based rendering, and Porter-Duff blend modes from the first two programming assignments.
Output: docs/core-rendering.html - complete documentation page with interleaved explanation, code, and demo.
</objective>

<execution_context>
@/Users/orases/.claude/get-shit-done/workflows/execute-plan.md
@/Users/orases/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-code-examples/05-CONTEXT.md
@.planning/phases/05-code-examples/05-RESEARCH.md
@graphics-engine/my_canvas.cpp
@graphics-engine/blend_functions.h
@graphics-engine/Edge.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Core Rendering documentation page</name>
  <files>docs/core-rendering.html</files>
  <action>
Create docs/core-rendering.html with the following structure:

**HTML Head:**
- Title: "Core Rendering (PA1 + PA2) - Graphics Engine Documentation"
- Meta viewport for responsive
- Prism.js CDN resources (same as test-assets.html)
- Local CSS: docs.css, code.css
- Local JS: tabs.js (defer)

**Header:**
- Navigation back to docs index (docs/index.html) and portfolio (../index.html)
- Title: "Core Rendering"
- Subtitle: "PA1: Lines and Blending | PA2: Polygons and Clipping"

**Introduction section:**
- Brief overview of what PA1 and PA2 accomplish (2-3 paragraphs)
- Foundation for all rendering: shapes become edges, edges become scanlines, scanlines become pixels
- This page covers: rectangle/shape drawing, edge rasterization, alpha blending, polygon clipping

**Tabs structure with 4 tabs:**

**Tab 1: "Edge Rasterization"**
- Explain the edge-based rendering approach
- How shapes are converted to a list of edges
- Scanline algorithm: for each row, find edge intersections, fill between
- Code snippet: Edge struct/class construction from Edge.h
- Code snippet: Edge walking logic (currX, slope, advancing per scanline)
- Highlight the x += slope calculation
- Extract 20-40 lines showing edge construction and update

**Tab 2: "Rectangle Drawing"**
- Explain drawRect() as the simplest case
- Rectangle to 4 edges conversion
- Clipping to bitmap bounds before rasterization
- Code snippet: drawRect() or rectangle handling from my_canvas.cpp
- Highlight the bounds checking and blitRow call
- Extract 15-30 lines

**Tab 3: "Porter-Duff Blending"**
- Explain premultiplied alpha and why graphics engines use it
- Porter-Duff compositing: how source and destination combine
- The blend equation: result = S_coef*S + D_coef*D
- Code snippet: src_over_mode (most common blend) from blend_functions.h
- Code snippet: 1-2 other modes (dst_over, xor, or clear) showing variety
- Highlight the alpha math: sa + div255((255-sa)*da)
- List all supported blend modes with brief descriptions

**Tab 4: "Polygon Clipping"**
- Explain why clipping is needed (shapes can extend outside viewport)
- Sutherland-Hodgman or similar clipping algorithm concept
- Code snippet: clip() or edge clipping from Edge.h or my_canvas.cpp
- Highlight boundary checks (top, bottom, left, right)
- How convex polygon guarantee simplifies clipping

**Demo section:**
- Heading: "Try It: Shapes and Blend Modes"
- Instruction: "This demo runs the C++ engine compiled to WebAssembly. Try different shapes and blend modes to see the edge rasterization and blending in action."
- iframe: src="../wasm-graphics-demo.html#demo=blend-modes" loading="lazy" title="Blend modes demo" width="100%" height="600"
- The #demo=blend-modes hash lets visitors see blending directly; could also link to #demo=shapes for basic shape drawing

**Footer:**
- Text: "Continue exploring:" with placeholder links to other doc pages
- Link back to docs index and main demo

**Code block requirements:**
- Use class="line-numbers" on pre elements
- Use data-line="X-Y" for highlighted lines (identify key algorithm lines)
- Use class="language-cpp" on code elements
- HTML-escape all angle brackets in C++ code (&lt; &gt;)
- Read actual source files to extract real code snippets
  </action>
  <verify>
Open docs/core-rendering.html in browser:
1. Page loads without errors (check console)
2. All 4 tabs functional (click switches content)
3. Code has syntax highlighting (colored keywords)
4. Line numbers display on left side
5. Highlighted lines have yellow/gold background
6. Copy button appears in code toolbar
7. Demo iframe loads wasm-graphics-demo.html#demo=blend-modes
8. Iframe shows blend modes demo with shape drawing
  </verify>
  <done>
Core Rendering page is complete with PA1+PA2 content: edge rasterization, rectangle drawing, Porter-Duff blending, and polygon clipping documented with actual C++ code and working demos.
  </done>
</task>

</tasks>

<verification>
All checks for this plan:
1. docs/core-rendering.html exists with 250+ lines
2. Page includes all CDN and local asset references
3. Four tabs with distinct content (Edge Rasterization, Rectangle Drawing, Porter-Duff Blending, Polygon Clipping)
4. Each tab has explanatory text + C++ code snippet
5. Code blocks have line-numbers class
6. At least one code block has data-line highlighting
7. Iframe src includes #demo= hash parameter
8. Demo iframe loads and shows appropriate preset
</verification>

<success_criteria>
- docs/core-rendering.html displays complete documentation for PA1 and PA2 content
- C++ code snippets are extracted from actual my_canvas.cpp, blend_functions.h, Edge.h files
- Syntax highlighting, line numbers, and copy work correctly
- Demo iframe loads blend modes preset via URL hash
- Code-to-demo mapping: visitor reads about blending, then sees blending in demo
- Page is readable and explains algorithms clearly to technical audience
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-examples/05-02-SUMMARY.md`
</output>
